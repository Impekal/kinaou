# KINAOU — PROJECT STATE & HANDOFF

Last updated: 2026-09-20

## Purpose
This is KINAOU's durable project-memory / handoff file. Update it after every meaningful merged slice so a new chat/session can continue without relying on conversation memory.

## Product identity
**KINAOU** is a local-first AI Creative Studio / Creative Operating System.
Core promise: **AI does the work. You stay in control.**
Long-term flow: Discover → Research → Understand → Script → Direct → Generate → Edit → Adapt → Publish → Measure → Learn.

## Non-negotiable decisions
1. Local-first; cloud/API providers are optional adapters, never mandatory.
2. No paid API, cloud GPU, subscription or production deployment without explicit user approval.
3. Non-destructive project/timeline/EDL is source of truth; MP4 is output only.
4. AI edits must be structured, reversible and manually overridable.
5. Models/workers are swappable capability-based adapters.
6. External SSD support is fundamental; never require formatting/clearing it.
7. Existing SSD files/folders are never touched. Automatic operations stay inside `<SSD>/KINAOU/`.
8. Models, Projects, Assets, Cache, Temp, Renders, Archive remain independently relocatable.
9. Missing storage must not corrupt projects; assets can become offline.
10. Start hardware: MacBook Pro M2 Pro, 16 GB unified memory, 512 GB SSD. New hardware is not a prerequisite.
11. Long videos are scene/asset compositions; individual scenes can be regenerated.
12. Preferred UI is web/PWA; trusted filesystem/FFmpeg/model execution is delegated to localhost worker/desktop bridge.
13. The app is private and non-commercial. Optimize freely for that use case, prefer capable local/open components, retain model/voice provenance, and do not implement license or terms circumvention.
14. Faceless production is a first-class path: scripts, local voices, attributable avatars/generated presenters, screen capture, generated visuals and editing must form a complete workflow without requiring the user's face or voice.
15. Initial content languages are German, English and French. Language, country/market, audience and objective are explicit project data rather than prompt guesswork.
16. Trend, search-demand and content-gap claims must retain source, observation time, geography, language and measurement type. KINAOU must never fabricate search volume, competition or likely views; no feature may promise virality or guaranteed outcomes.
17. Avatar requires three explicit modes: photo-conditioned, video-conditioned and fully generated fictional identities without a real-person reference. All target photorealistic performance with identity consistency, believable movement/expression and audio-driven lip sync, not a static portrait. Higgsfield is the user's quality reference, not an authorized paid/cloud dependency. The current voiced-still slice is only an interim composition tool; extracting a frame is not equivalent to true video conditioning.
18. Speech quality must be natural and expressive in German, English and French: appropriate emotion, emphasis, pacing, pauses and pronunciation, without robotic artefacts. Piper is a baseline adapter, not the final quality bar. Evaluate actual outputs and report hardware/model limitations honestly.
19. Full course production is a first-class target: coherent modules/lectures, instructor expertise and learning outcomes, demonstrations, scripts, narration, captions, exercises/quizzes with reviewed answers, downloadable material, revisions and per-lecture exports. Udemy preparation must follow current platform rules and must not claim approval or publish automatically.
20. SSD use is not required for independent repository work and temporary-fixture tests. It is the intended production store for large models, projects and media as well as real hardware testing. Notify the user before a necessary SSD action; never silently relocate or overwrite their files.
21. The entire application UI must support German, English and French independently of content/course language. Existing content-language metadata does not fulfill UI localization. Cover navigation, controls, help, validation and job states; keep user-authored content unchanged.
22. Main YouTube videos and related Shorts should be proposed together, with an explicit local publication plan. Label timing suggestions with audience region/timezone and their evidence; absent channel analytics, provide reviewable test hypotheses rather than fabricated best-time/virality claims. No automatic remote publication is authorized by a local recommendation.
23. Support cartoons, engaging age-appropriate children's videos and educational content, not only realistic presenters or adult courses. Optional trend research must retain dated attributable evidence and never turn local-model guesses into current demand claims. Children's content needs an explicit age/learning target and content/factual review; cartoons are not automatically suitable for children.
24. The requested production path must not depend on recurring subscriptions, paid inference or cloud APIs. Prefer compatible local/open runtimes and authorized assets; disclose real storage, hardware and electricity requirements. No promise of zero operating costs, silent downloads or license circumvention.

Detailed acceptance criteria and source-backed platform constraints: [CREATIVE_QUALITY.md](CREATIVE_QUALITY.md).

## Storage
```text
<external SSD>/
├── existing user folders (untouched)
└── KINAOU/
    ├── Models/
    ├── Projects/
    ├── Assets/
    ├── Cache/
    ├── Temp/
    ├── Renders/
    └── Archive/
```
Safety rule: only paths below the configured KINAOU root may be created/moved/deleted automatically.

## Repository
Repo: `Impekal/kinaou`
Default branch: `main`
Current main SHA after PR #217: `b011765b7f9ae9c03a2c1b119feed723ba81f4f8`

## Merged slices
- **PR #217** — DE/EN/FR Short archive and scoped presence checks (internal substep 1.16). Summaries, terminal states, dates, original saved names, review/forget actions and check feedback are translated. Each recorded file is explicitly unchecked until read; historical export success is not current presence or integrity. Scope lifetimes reject late responses after project/connection/path changes, including returning to earlier values, and unmount. Existing validated 50-path requests cover up to 100 paths; a later chunk failure publishes no partial-success counts. Forgetting catches persistence errors and changes metadata only; archive review clears obsolete recipe-review feedback without submitting work. Full gate: 533/533 app + 82/82 native tests + build/syntax green. Ten regressions cover locale/original names, metadata preservation, authenticated bounded chunks, partial failure and actual scope identities. Isolated localhost:5195 checked two present files plus one absent path with a real worker, DE→FR→EN retention of expanded archive/check time, unchecked/disconnected state after reload, one candidate × three formats restored only for review, and persistent summary removal. Both MP4 SHA-256 values remained identical; console clean; temporary processes stopped. No worker source, SSD, user media, models or deployments changed. Active Short batch lifecycle/localization and persistence/recovery review remain next; whole point 1 is open.
- **PR #215** — DE/EN/FR exact Short preview and isolated sessions (internal substep 1.15). Format/help/job/cancel/retry/playback feedback follows UI language without restarting or losing playhead. Every submission gets a unique cache path; scope includes the full prepared plan and connection, so edits/unmount/connection changes detach late start/status/file results. Serial polling, same-job/file recovery and cancellation epochs prevent overlapping status reads and stale cancel races. Unknown start acceptance requires a deliberate new submission. Full gate: 523/523 app + 82/82 native tests + build/syntax green. Seventeen added regressions include actual independent 1s/540×960 red/blue previews, authenticated file equality and unchanged source bytes. Isolated localhost:5194 verified DE→FR while queued, EN playhead retention at 1.25s in a 2s portrait preview, format independence, loaded and pending preview invalidation after a transform, four independent cache files and honest session clearing after reload. A fast browser cancel raced completion and correctly loaded actual success; confirmed cancellation, failed cancellation and detached late responses are covered by tests. Console clean; temporary processes stopped. No worker source, SSD, user media, model or deployment changes. Batch/archive lifecycle localization and recovery review remain; whole point 1 is still open.
- **PR #213** — DE/EN/FR Short selection, exact limits, format choices, saved recipes and review-only recovery notices (internal substep 1.14). Language changes retain current choices and name drafts; original scene/recipe names remain unchanged. Typed skip reasons retain the exact fractional limit instead of rounding to whole seconds; guidance distinguishes local editing candidates from platform eligibility, trend evidence or success promises. Maximum-save failures are caught. The browser test exposed absolute worker output paths breaking durable batch persistence; a validated job-result adapter now binds job identity/output, stores the planned portable path and rejects mismatched or incomplete success. Start, poll and cancellation results use it. Full gate: 506/506 app + 82/82 native tests + build/syntax green. Eight added regressions include an actual FFmpeg render whose receipt survives serialization/reload/archive. Isolated localhost:5193 checked DE→FR→EN drafts/choices, invalid and 1.501s limits, missing recipe candidates after regrouping, explicit archive reuse, and new actual 2s landscape/square MP4s (1920×1080 and 1080×1080). Both new terminal states survived reload and archival; source SHA-256 stayed identical. An earlier run exposed the original persistence/update-loop fault; no new console errors occurred in the corrected run. Test processes stopped. No worker source, SSD, user media, models or deployment changes. Short preview/batch lifecycle and archive controls still need localization and scope/recovery review; whole point 1 remains open.
- **PR #211** — DE/EN/FR course-export review and stale-selection guard (internal substep 1.13). Lesson controls, range eligibility, corrupt-outline help and explicit review/approval boundaries are translated while saved names stay original. Selection captures project, lesson range and course context/revision; a changed or missing selection blocks single export instead of silently becoming a custom-range render. Explicit re-review or clearing restores control; manual In/Out, Whole timeline or Short selection deliberately release course attribution. Full gate: 498/498 Vitest + 82/82 native tests + build/syntax green. Sixteen new regressions cover review identity/revision/range changes, removed/out-of-bounds lessons, three-language recovery and stable range codes. Existing real-worker lesson export tests remain green. Isolated localhost:5192 browser verified DE→FR→EN retention, disabled future range, a timeline trim causing a blocked export, explicit re-review, actual 2s/1920×1080 lesson MP4 and exact 1–3s receipt retaining original names/revision/French course language, manual-range deselection and receipt persistence after reload. Console clean; test processes stopped. No worker source, SSD, models, user media or deployment changes. Review remains session-only; this is not semantic validation of teaching material or complete localization of Shorts/Studio.
- **PR #209** — DE/EN/FR framing and export history (internal substep 1.12). Independent format controls and saved export summaries follow UI language while authored/stored identities stay original. Framing and receipt-removal persistence failures are caught locally. File checks distinguish unchecked/present/missing, state their time and presence-only limits, serialize requests and ignore stale results after project/connection/path changes or unmount. Full gate: 482/482 Vitest + 82/82 native tests + build/syntax green. Sixteen new regressions cover translated controls, persisted format independence, escaped original labels and check-session edge cases. Executing export integration verifies present/missing files and byte-identical MP4s after forgetting a receipt; native FFmpeg reframing proves actual source-region selection. Isolated localhost:5191 browser verified DE/FR/EN retention, true file counts, two 1s vertical exports with red/blue centre pixels, reset, receipt removal with unchanged MP4 SHA-256, reload persistence and cleared disconnected file checks. Console clean; test processes stopped. No worker source, model, SSD, user-media or deployment change. This is not whole-point-1 completion; remaining Shorts/course export and specialist panels stay on the localization list.
- **PR #207** — DE/EN/FR single-export lifecycle and base controls (substep 1.11). Format/range/audio options, readiness and start/progress/cancel/error feedback follow UI language without restarting the job. Each submission captures its own plan/receipt and unique file; serial polling and same-job/status/save retries avoid duplicate renders. Late detached responses cannot overwrite the project; observed success merges its original receipt into the latest same-project edits. Uncertain acceptance, failed receipt persistence and cancellation races are explicitly distinguished. Full gate: 466/466 Vitest + 82/82 native tests + build/syntax green. Twenty-one new regressions include two real authenticated FFmpeg exports with red/blue pixel, 1s duration, 1920×1080, independent receipt and unchanged-source assertions. Isolated localhost:5190 browser verified DE→FR range retention, EN switch while queued, a real 4s/1920×1080 MP4, confirmed cancellation of a second independent export, and receipt persistence after reload; console clean and test processes stopped. Framing controls, receipt-list/file-check controls and Shorts/course specialist controls remain partially English. Leaving Studio/changing connection stops monitoring, not accepted work; no automatic single-job recovery after reload. No worker source, SSD, models, user media or deployment changes.
- **PR #205** — DE/EN/FR scene narration and isolated job sessions (substep 1.10). Voice-language selection, scene skips, fit limits and job/save/error feedback follow UI language without rewriting speech. Request-bound serial sessions reject late results after edits/connection changes/unmount; known status/save failures retry the same job/result, uncertain submission is not automatically resubmitted. Only persisted scenes count as saved; earlier results survive later failures. Fit validates before safety history/persistence. Full gate: 445/445 Vitest + 82/82 native tests + build/syntax green. Twenty-three new tests include an authenticated real-worker execution using an explicitly synthetic tone CLI fixture: exact source text, measured 2s WAV, placement/provenance/overrun and repeat/no-overwrite checks. This is NOT Piper inference or voice-quality evidence. Isolated localhost:5189 browser verified DE→FR during queued work, EN saved results, silent scenes, repeat without duplicates, fit, safety restoration, late-result rejection after a manual edit and reload persistence; console clean. Test processes stopped. Detach stops UI monitoring/insertion, not accepted worker work; generated files remain. No worker source, model download, user media, SSD or deployment change. Piper remains a baseline, not expressive/own-voice synthesis.
- **PR #203** — DE/EN/FR storyboard assembly and replacement controls (substep 1.9). Stable skip codes expose unavailable replacements and escaped planning errors; locked tracks block both writes. Results appear only after validated changes are safety-snapshotted and persisted, including legacy clip-to-scene adoption; true no-op repeats add no history. Shortened replacements retain their start but require review of surrounding transitions/audio/captions, not an automatic realignment promise. Sub-100ms dissolves now use valid hard cuts. Full gate: 422/422 Vitest + 82/82 native tests + build/syntax green. Twenty new regressions cover localization, locks, calculation/storage failures, reversible changes and short scenes. Isolated localhost:5188 with temporary worker/media verified DE/FR/EN option retention, append/no-duplicate repeat, replacement shortening, safety restore, reload and clean console. Its real 5s/960×540 preview showed the blue replacement, expected black gap and untouched red original/appended scenes; browser playback loaded without error. Test processes stopped. No worker source, SSD, user media, models or deployments changed. This does not complete whole-app localization or add a new voice/avatar engine.
- **PR #201** — DE/EN/FR source/composed preview controls and isolated sessions (substep 1.8). Typed readiness reasons and lifecycle states distinguish request, queue, render, media loading, failure and loaded file; technical details remain escaped/original. Language changes retain playback, while source/edit/connection changes invalidate stale responses and release object URLs. Each composed submission has a unique cache output; serial polling and same-job/file retries prevent cross-plan loading and duplicate submissions after status/read failures. Unknown start acceptance and detached jobs are disclosed. Full gate: 402/402 Vitest + 82/82 native tests + build/syntax green. Twenty-three new tests include two actual concurrent FFmpeg previews with red/blue pixel, duration, 960×540 and authenticated-byte checks. Isolated localhost:5187 with a temporary worker verified source/composed playback, DE→FR during queued render, EN playhead retention, old-preview invalidation during a 3→4s clip edit, correct 4s playback, translated missing-file error and honest disconnected state after reload. Clean browser console; test processes stopped. No worker source, SSD, models, dependencies or deployments changed. This remains render-then-play; leaving/editing stops monitoring, not an already accepted worker job, and cache files are retained.
- **PR #199** — DE/EN/FR manual timeline controls and safe saved edits (substep 1.7). Track/clip controls, summaries, accessible position labels and help follow the selected language while authored names/media remain unchanged. The blanket AUTO-SAVED Studio badge is replaced by explicit saved-change/draft guidance; planning placeholders and schematic layout are disclosed. Each manual change is calculated/validated, then safety-snapshotted before persistence; errors do not report success. Source-overrun lengthening and speed resets are disabled, respecting source offsets, fade/transition spans and locks. Full gate: 379/379 Vitest + 82/82 native tests + build/syntax green. Isolated localhost:5186 browser verified planning-block creation, move/scale/fade, locking, DE/FR/EN switching, reload persistence and reversible safety-history restoration, with no console warnings/errors. Test server/tab stopped. No worker, SSD, model or media-file changes. This is not a scale-accurate drag editor or completion of whole-app localization.
- **PR #197** — DE/EN/FR script-caption generation and alignment feedback (substep 1.6). Stable per-scene reason codes translate skipped generation/alignment without parsing English diagnostics or changing names/speech. Previously hidden unalignable scenes and planning errors are now visible; locked tracks disable alignment. Saved results appear only after persistence succeeds and are hidden after unrelated project/target changes. Safety history remains before writes, and timing-only alignment retains all caption words. Full gate: 369/369 Vitest + 82/82 native tests + build/syntax green. Isolated localhost:5185 UI restored a dedicated metadata fixture through a temporary worker, generated a missing caption from narration, checked DE/FR/EN skip feedback, realigned an existing caption from 0–4s to 0–8s, and verified text/timing after reload with no browser warnings. Fixture visuals were explicitly offline; this was a metadata/UI test, not a new model/media-render quality claim. Test processes stopped; no user SSD, model or worker source changes.
- **PR #195** — DE/EN/FR manual caption authoring and reviewed transcript import (substep 1.5). Explicit text-save/revert controls replace silent blur writes; creation/edit/import catch failures and create safety versions before project persistence. Drafts survive language changes, pending text conflicts after a history restore are retained and blocked from overwriting the new saved text, and project IDs isolate drafts with distinct sibling keys. Creation rejects blank/non-finite/unsafe timing values, rounds to milliseconds and advances the next start using the newly saved track. Shared caption assets referenced on a locked track cannot be rewritten. Original transcript text, timing and provenance remain; import warns about absolute timing and repeat duplicates. Full gate: 357/357 Vitest + 82/82 native tests + build/syntax green. Isolated localhost:5184 browser verified DE/FR/EN drafts, save/edit/reload, safety history, restore conflicts, sequential start times and a clean console after fixing a duplicate React-key warning. No worker/SSD/model changes. Script-caption generation/alignment remain the next separate localization slice.
- **PR #193** — DE/EN/FR project backups and global recovery (substep 1.4). Backup controls, dates, in-flight/result/error feedback and app-error guidance follow the selected language; titles, paths and original diagnostics remain exact. Copy explicitly describes metadata-only JSON, same-project backup replacement, safety versions and unsaved-draft risk without asserting that an arbitrary error could never affect data. Connection/project changes and unmount invalidate late responses before they can restore browser state; successful saves followed by failed listing have a distinct result. The language-aware inner error boundary retains an independent outer fallback for provider/storage failures. Full gate: 335/335 Vitest + 82/82 native tests + build + syntax green. Isolated localhost:5183 browser testing used a real worker with temporary storage: save, DE/FR/EN feedback, restore an earlier course-less project, verify its safety version, and translate an offline-list error. Worker and browser test processes were stopped. No SSD/model/live-worker changes; this does not add media-file backup or complete all UI translation.
- **PR #191** — localized Version History and honest recovery feedback. Controls, counts, dates/source summaries, confirmation and action results follow DE/EN/FR while saved labels and current name drafts remain unchanged. Read failures no longer masquerade as empty history; explicit refresh is available. Snapshot/restore/delete failures are caught; restoration success is shown only after the project callback succeeds, and failure copy acknowledges a possible retained safety version. Draft state is isolated by project ID, not language. Full gate: 319/319 Vitest + 82/82 native tests + build + syntax green. Isolated localhost:5182 browser test verified draft retention across languages, real snapshot save, changed-course restoration, return through the safety version and persistence after reload. No worker/media-file operations. This completes substep 1.3, not the whole UI milestone.
- **PR #189** — German/English/French Settings with honest local connection/storage behavior. Parent-owned endpoint/token/storage drafts survive language switches, endpoint fields lock during an active connection check, connection errors have translated summaries with escaped technical details, and storage saves report success or failure without falsely presenting a path preference as a mounted drive or changed worker root. Capability IDs and paths remain exact; tokens remain memory-only. Full gate: 311/311 Vitest + 82/82 native tests + build + syntax green. Isolated localhost:5181 browser verification covered all three languages, draft retention, invalid-URL rejection, dynamic result messages, profile persistence and empty token after reload. No worker, SSD, runtime, dependency or deployment change. This completes substep 1.2, not the whole UI milestone.
- **PR #187** — persisted German/English/French application-language foundation. A native-name sidebar selector, browser-language fallback and document language are separate from content/course language. Navigation, project creation/library summaries, course forms and course validation/recovery summaries are translated; user text, timeline/media, identifiers and drafts are not rewritten or remounted by a language change. Preference-write failure keeps the session choice with an honest warning. The interface explicitly marks specialist panels and technical details as still English, so this is not complete app localization. Complete CI: 301/301 Vitest + 82/82 native tests + build + syntax green. An isolated real-browser test verified DE/FR/EN switching with project/course drafts preserved, separate course language, dynamic validation messages, save/discard and reload persistence. No worker change, download, dependency or paid/cloud call. Scope and extension rules: [UI-LANGUAGES.md](UI-LANGUAGES.md).
- **PR #185** — persistent course/module/lesson authoring and independent real lesson exports. Course adds a bounded, revisioned outline with German/English/French metadata, audience/prerequisites/outcomes, module/lesson identities, objectives and absolute timeline ranges. Explicit validated saves create a safety version; malformed saved data and stale drafts are refused. Studio offers review-only lesson selection followed by the normal explicit worker render. Each output gets a fresh path; successful receipts retain the submitted course/module/lesson titles, IDs, language and revision independently of subsequent edits. Original assets/timeline/earlier MP4s stay untouched. Complete CI: 289/289 Vitest + 82/82 native tests + build + syntax green. Real FFmpeg tests verify two separate retimed lesson videos, selected red/blue pixels, durations, audible tail audio and unchanged earlier output. This is not complete course generation, instructor review, a materials package or platform approval. Workflow/limits: [COURSE-PRODUCTION.md](COURSE-PRODUCTION.md).
- **PR #183** — authorized portrait and own-speech inputs for local video workflows. Managed video templates may explicitly declare core LoadImage/LoadAudio bindings; Video Studio requires selecting and authorizing each declared reference, including the user's original speech recording without a TTS requirement. The worker transfers bounded regular files to localhost ComfyUI without redirects or overwriting, binds the uploaded filenames before submission, and preserves source IDs/paths/authorization plus hashes of actual uploaded bytes on generated assets. Linked/unsafe/missing/oversized inputs fail; cancellation aborts uploads. UI discloses retained ComfyUI copies, permission requirements and the installed-model boundary. No bundled animation, cloned speech, video-reference conditioning or quality claim. Final complete CI gate: 280/280 Vitest + 82/82 native worker tests + build + syntax green. Real-worker HTTP tests use PNG/WAV and FFmpeg MP4 fixtures, not simulated claims of AI inference. Contract and optional model test: [VIDEO-REFERENCES.md](VIDEO-REFERENCES.md).
- **PR #181** — preserve actual scene speech through Director, TTS and captions. Previously Director's narration remained only in archived plan metadata while live scenes spoke/captioned their visual descriptions. Newly applied plans now retain bounded optional narration in project truth. One resolver feeds TTS and script captions, explicit empty narration means silence, and absent fields retain legacy behavior. Review shows speech separately from visual directions; voice/caption assets retain the correct text-source provenance. Existing recordings/captions are never automatically replaced, and old projects are not silently migrated. German/English/French regression tests cover apply, serialization, TTS request body, voice registration, captions and render-plan text, plus silence/fallback/review validation. Final gate: 276/276 Vitest + 77/77 native worker tests + build + syntax green.
- **PR #179** — real reversible voiced-portrait presenter composition. Avatar now offers an explicitly labelled still portrait plus existing narration, not an animated/lip-synced engine. Select online managed image/audio assets, review exact timing and append two ordinary editable avatar/voice tracks after every existing clip, including muted/locked tracks. The original assets and their source metadata are unchanged; a pre-apply Version History snapshot makes the addition reversible. The portrait lasts for the full measured recording. Unsafe paths, offline/wrong-kind sources, unknown/invalid/over-one-hour audio durations and a resulting timeline over one day are rejected. A real-worker integration test verifies visible pixels, audible late audio, exact dimensions and full duration for landscape export and portrait preview. Final CI gate: 269/269 Vitest + 77/77 native worker tests + production build + syntax green; isolated local UI empty-state and disabled-action check passed.
- **PR #177** — language-aware local Piper voice discovery. The authenticated voice endpoint retains legacy paths and adds declared locale metadata from adjacent installed configurations, bounded to 200 reads and 64 KiB each, rejecting config symlinks and treating missing/malformed metadata as unknown. Audio and scene narration show project-language matches/differences/unknowns, require explicit voice selection and discard stale discovery responses. Filenames never imply language; old workers remain usable with unknown labels. No model execution/download, quality or multilingual guarantee is implied. Final CI gate: 264/264 Vitest + 77/77 native worker tests + production build + syntax green. The local FFmpeg installation lacks the subtitles filter, so its existing caption smoke test fails locally; the complete GitHub FFmpeg gate passed unchanged.
- **PR #1** — foundation. Main `0230448aad6ee49e98118ab196b91f2b20e8ca8a`. React/Vite/TS, project schema, non-destructive timeline, storage safety, versioning, jobs, model registry, CI.
- **PR #2** — product core. Main `581cd2a18a7d963f6bf0c630d797716ef3a8d6dd`. Persistent projects, Create flow, standard tracks, timeline persistence, storage profiles.
- **PR #3** — media core. Main `bb93d8d0374ee1a32f2e1d76346d9b2ae91918a4`. Asset registry/offline state, worker scheduling, deterministic safe render plans.
- **PR #4** — local worker contract. Main `c53c720936624b0e208aaa339d63f55d489a7e6f`. Worker RPC, managed-root mapping, ffprobe/ffmpeg planning.
- **PR #5** — startable Mac worker. Main `3677d3994a143da9f88442eb004fe278dd31dfe7`. Authenticated localhost Node worker, real ffprobe, first real ffmpeg, `shell:false`, KINAOU-only paths.
- **PR #6** — PWA worker bridge. Main `c71efbc1eb7d176abdd08b264ecd39acdde0e82d`. WorkerClient, in-memory token, managed media import.
- **PR #7** — visible worker + Assets UI. Main `92e6069547a79e9d62e220edb0e2794ad8fb360c`. Worker Test Connection/capabilities, real managed media probe/import. Final gate 28/28 + build + worker syntax green.
- **PR #8** — async render jobs + multi-track compositor. Main `7d2cc05b975d77057dc6a95b79fe30e0f0ef376e`. queued/running/succeeded/failed/cancelled jobs, progress/status/cancel, visual layering and voice/dialog/music/SFX mixing. Speed != 1 explicitly blocked. Push + PR gates green.
- **PR #9** — visible Studio render lifecycle. Main `48bd07bd5b04125b7d7d98a26d680ec42fe5bea1`. Safe output naming, readiness, start/poll/cancel/result/retry UI. Unsupported captions, planning/external/offline assets and speed changes blocked. Push + PR gates green.
- **PR #10** — real media placement + timeline controls. Main `22589ca9ca6f9ed9ed53c8799e6865f075c41976`. Managed asset → compatible selected track; probe duration/still defaults; mute/lock/move/trim/remove/audio gain. Locked-track enforcement. Push + PR gates green.
- **PR #11** — secure browser file import + deterministic visual z-order. Main `576dc633a5987bf819ccb79f0cc0754414587691`. Explicit browser File/Blob streaming to authenticated worker; worker receives bytes, never arbitrary source path. Upload writes `.part` under `KINAOU/Temp/Uploads`, enforces configurable size limit, sanitizes/collision-proofs destination, atomically renames to `KINAOU/Assets`, cleans failures, then PWA probes/registers asset. Worker advertises `asset-upload`. Native `node:test` worker security tests were separated from Vitest discovery. RenderPlan now carries `trackIndex`; higher visual track indexes composite later/on top independent of clip start order. Final push + PR gates: Vitest + native worker tests + production build + worker syntax all green.
- **PR #12** — CI notification noise + reproducible installs. Main `f13c9d349f478b1bbac0d90eae93461e0e0b4f31`. Feature work is gated once through `pull_request`; `push` CI now runs only on `main`, so intermediate work-in-progress commits no longer generate repeated failure notifications or duplicate PR checks. Superseded runs are cancelled, workflow permissions are read-only, `package-lock.json` is committed and CI uses `npm ci`. Final local gate: 40/40 Vitest + 2/2 native worker tests + production build + worker syntax. PR gate green.
- **PR #13** — supported GitHub Actions runtime. Main `b31f4ce3b504707c26b483505fd6a7101f9d10cb`. `actions/checkout` and `actions/setup-node` moved to v5, removing the deprecated Node 20 action-runtime warning while the application test matrix remains on Node.js 22. Local and PR gates green.
- **PR #14** — structured captions and real subtitle rendering. Main `c8bb9c86d748dab82ec499492c6a11e17c693719`. Studio creates and edits timed caption assets as project metadata; caption clips pass readiness without pretending to be filesystem assets. The worker deterministically generates UTF-8 ASS under `KINAOU/Temp/Captions`, burns it in after visual composition, neutralizes ASS override characters, supports Unicode/multiline text and removes the temporary file after success, failure or cancellation. Final gate: 43/43 Vitest + 4/4 native worker tests + production build + worker syntax green.
- **PR #16** — persistent track/layer reordering. Main `6bc069e663b141029fe247cf8a8eaa502badf11b`. Studio exposes guarded Layer Up/Down controls; immutable timeline operations persist order and the existing `trackIndex` compositor contract makes it the real visual z-order. Final gate: 44/44 Vitest + 4/4 native worker tests + production build + worker syntax green.
- **PR #17** — render-backed clip transforms. Main `c3ea67d513a7320ed7ed748aac90a3ff16e5595c`. Backward-compatible clip metadata and Studio controls now drive position, 0.1–4× scaling, symmetric pixel crop and reset. Both the typed worker contract and actual Mac FFmpeg compositor validate and apply the same filter graph. Final gate: 46/46 Vitest + 4/4 native worker tests + production build + worker syntax green.
- **PR #19** — render-backed dissolve transitions. Main `62ffcb35f2a11193ab7302c329a1f2f932f12746`. Visual clips can add/remove a bounded 100–5000ms dissolve-in; timeline validation ensures the transition fits the clip, and both render implementations apply a real alpha fade before overlaying the clip at its timeline position. Final gate: 48/48 Vitest + 4/4 native worker tests + production build + worker syntax green.
- **PR #21** — render-backed audio/visual fade envelopes. Main `f1f37148ae4ad0cf9f7b224053bef3ef2a429d6d`. Clips persist bounded fade-in/out automation; Studio exposes reversible controls, the compositor applies alpha fades to visual layers and `afade` to audio before timeline placement, and validation prevents envelopes exceeding clip length. Final gate: 50/50 Vitest + 4/4 native worker tests + production build + worker syntax green.
- **PR #23** — exact audio/video speed retiming. Main `9a78142cfac39d7a429f54a64ae6839cbbe1ae2d`. Video/audio clips support 0.25×–4× speed while timeline `durationMs` remains the exact output duration. Worker input reads `duration × speed`, video PTS is rescaled, audio uses safe chained `atempo` plus exact trim, fades/delay remain clip-local, and known source bounds are enforced. Images/captions reject meaningless speed changes. Final gate: 53/53 Vitest + 4/4 native worker tests + production build + worker syntax green.
- **PR #25** — managed video proxy generation. Main `f413013e5475b8ce12f5b18da30c4e66c9a04243`. Authenticated worker generates deterministic 960px H.264/AAC proxies only from `KINAOU/Assets` into `KINAOU/Cache/Proxies`; Studio Assets exposes generation and persists the proxy association on the original asset without replacing its full-quality render URI. Worker advertises `media-proxy`. Final gate: 55/55 Vitest + 6/6 native worker tests + production build + worker syntax green.
- **PR #27** — authenticated Studio proxy playback. Main `0ffd7fa272f286a29529ced62e62a9163ed04def`. Worker streams only generated MP4 files below `KINAOU/Cache/Proxies`; the browser sends the token only in the Authorization header, creates a short-lived object URL and revokes it on replacement/unmount. Studio labels this as source preview rather than composed output; final render still consumes original asset URIs. Final gate: 56/56 Vitest + 7/7 native worker tests + production build + worker syntax green.
- **PR #29** — managed video thumbnails. Main `7f06845ace383bf0081feae52379a1842bd4509e`. Worker extracts deterministic 640px JPEG poster frames from managed videos into `KINAOU/Cache/Thumbnails`, advertises `media-thumbnail`, and streams only validated preview-cache paths through the authenticated media route. Assets persists and displays the thumbnail without changing the source. Final gate: 58/58 Vitest + 8/8 native worker tests + production build + worker syntax green.
- **PR #31** — managed audio waveforms. Main `01d4afd6ce669a5fe2374f29ead47977e5dad144`. Worker uses FFmpeg `showwavespic` to render deterministic mono amplitude PNGs under `KINAOU/Cache/Waveforms`, advertises `media-waveform`, and serves them only through the authenticated preview route. Audio/video assets persist the association; Assets displays it and audio timeline clips render the waveform. Final gate: 60/60 Vitest + 9/9 native worker tests + production build + worker syntax green.
- **PR #33** — composed timeline preview with scrubbing. Main `0e86028db388fbc551039f4c3f5f8cf2021ff7e1`. Render plans explicitly distinguish `export` and `preview`; the worker enforces purpose/path pairing, uses the real compositor at 960×540 for preview jobs under `KINAOU/Cache/Previews`, and exposes the result only through authenticated preview media transport. Studio polls progress and provides video playback plus a precise playhead slider. Original media and full export semantics remain unchanged. Final gate: 62/62 Vitest + 9/9 native worker tests + production build + worker syntax green.
- **PR #35** — persistent reversible Version History UI. Main `b4aa43996bb729e0ff191d50a0f3ef2c541718c9`. Studio creates, lists, restores and deletes named project snapshots persisted alongside the project in local storage. Restores first capture the current project as an automatic safety version, stored payloads are schema-validated and corrupt entries are ignored, and retention is capped. Final gate: 64/64 Vitest + 9/9 native worker tests + production build + worker syntax green.
- **PR #37** — validated DirectorPlan review/apply workflow. Main `aa8ee9bec52a03a01c6592264cdee48019449211`. A bounded versioned schema validates script, unique scenes, media requirements and human/local-model provenance. Director exposes real JSON validation and scene review, applies accepted script/storyboard data to project truth only after review, persists the source plan, and creates an automatic safety version first. It explicitly does not claim model generation. Final gate: 67/67 Vitest + 9/9 native worker tests + production build + worker syntax green.
- **PR #39** — real localhost Ollama Director adapter. Main `62ae58219bdccc73953f8914d499500376e50b92`. The worker permits only loopback Ollama URLs, detects actually installed models through `/api/tags`, advertises local-LLM capability only when available, and requests non-streaming schema-structured plans at temperature zero. It stamps trusted adapter/model provenance; the PWA validates the result again and requires scene review before reversible apply. No model download or cloud endpoint is used. Final gate: 68/68 Vitest + 12/12 native worker tests + production build + worker syntax green.
- **PR #41** — safe whisper.cpp STT contract. Main `ad3128af63883e493294dfcdbde45ae49d813111`. Model discovery accepts only GGML files under `KINAOU/Models`; deterministic shell-free commands convert managed media to 16-bit/16-kHz mono WAV and invoke configured whisper-cli JSON output. Temporary and durable paths stay under managed STT/transcript directories, and timestamped whisper.cpp JSON is normalized into a stable attributable transcript schema. UI remains hidden until the cancellable runtime job is complete. Final gate: 68/68 Vitest + 16/16 native worker tests + production build + syntax green.
- **PR #43** — cancellable local whisper.cpp STT jobs. Main `776773c4432d00b99693fa6ab1c2cf86c837f3ad`. The authenticated worker exposes model discovery and start/status/cancel endpoints, advertises speech-to-text only when FFmpeg, an absolute configured whisper-cli and a managed GGML model are available, accepts only managed Assets input, executes conversion and inference shell-free, normalizes durable JSON under `KINAOU/Projects/Transcripts`, and removes temporary WAV data after every terminal path. Final gate: 70/70 Vitest + 16/16 native worker tests + production build + worker syntax green.
- **PR #45** — visible local STT Assets workflow. Main `cc01f5fa4aa0ff1aff643c5e43e988de7aa9afbf`. Assets exposes available managed audio/video sources, detected GGML models and language selection; it starts, polls and cancels authenticated STT jobs and displays progress/result. Successful transcripts are registered exactly once as managed document assets with source, job, adapter, language and normalized segments retained as attribution. Final gate: 72/72 Vitest + 16/16 native worker tests + production build + worker syntax green.
- **PR #47** — reviewed transcript segments to real captions. Main `e5a543946d41c55cafb8c493d75ee52dbcebdbd5`. Caption Studio discovers only structurally valid transcript assets, exposes every timestamped segment for explicit selection, and creates caption assets/clips at the original STT times. Each caption retains transcript, segment and whisper.cpp attribution; corrupt selections and locked tracks are rejected before mutation, and an automatic Version History snapshot makes each batch reversible. Final gate: 74/74 Vitest + 16/16 native worker tests + production build + worker syntax green.
- **PR #49** — safe local Piper TTS contract. Main `7a61c359643c7a051cb0047362303fc7c88fc8d5`. Voice discovery accepts only ONNX models with adjacent configuration under `KINAOU/Models`; synthesis text is trimmed and bounded, temporary text and generated WAV targets stay in managed directories, and the current Piper file-input command is constructed without a shell. UI remains hidden until cancellable execution exists. Final gate: 74/74 Vitest + 19/19 native worker tests + production build + syntax green.
- **PR #51** — cancellable local Piper TTS jobs. Main `8abcc5802187f6dfc7857f424e66af5ac1b97454`. The worker advertises text-to-speech only when Piper, ffprobe and a managed configured ONNX voice are available; authenticated endpoints discover voices and start/poll/cancel jobs. Piper runs shell-free from a managed temporary UTF-8 text file, successful WAV output is probed and retained under `KINAOU/Assets/GeneratedVoice`, while temporary text and incomplete audio are removed after failure or cancellation. Final gate: 76/76 Vitest + 19/19 native worker tests + production build + worker syntax green.
- **PR #53** — real local Piper Audio Studio workflow. Main `f41b7951ffbdf5b0f893a0ada7ce4b9e1fd40cd5`. Audio is now functional: it detects managed Piper voices, starts/polls/cancels synthesis, displays honest availability and progress, registers successful WAVs exactly once with adapter/voice/job/source-text attribution, and exposes explicit compatible timeline placement. Final gate: 78/78 Vitest + 19/19 native worker tests + production build + worker syntax green.
- **PR #55** — reviewable reversible AI Editor proposals. Main `7eb89e40554c47c0767bee4cc1f7c9404d0ac623`. A bounded versioned schema supports safe move, trim, gain, speed, fade and caption-text edits. Studio validates every referenced target, shows concrete before/after values, requires explicit per-operation selection, applies only the chosen subset, records accepted proposal provenance and creates an automatic Version History safety snapshot. No model output is claimed yet. Final gate: 80/80 Vitest + 19/19 native worker tests + production build + worker syntax green.
- **PR #57** — local Ollama AI Editor generation. Main `bd8559adc12963532b43ba18cb2674944780aa87`. Studio sends only a bounded sanitized timeline context without media URIs or unrelated metadata to the localhost adapter, requests schema-constrained operations at temperature zero, overwrites claimed provenance with trusted model/adapter identity, and revalidates every returned target before showing the existing per-operation diff/review flow. Final gate: 81/81 Vitest + 20/20 native worker tests + production build + worker syntax green.
- **PR #59** — safe local ComfyUI workflow contract. Main `841cd00742db19d4003e6c727cb359683333fc85`. Only loopback HTTP endpoints and API-format JSON wrappers below `KINAOU/Models/ComfyUI/Workflows` are accepted. Templates explicitly declare which prompt, seed and optional dimension inputs KINAOU may replace; workflows are copied before binding, parameters are bounded, and generated-image destinations are reserved below `KINAOU/Assets/GeneratedImages`. Runtime/UI remain hidden until cancellable managed execution exists. Final gate: 81/81 Vitest + 25/25 native worker tests + production build + syntax green.
- **PR #125** — captions follow the scene they belong to. Main `e076eeeb69ee98187da31b8e44b4ca8e5da42499`. Captions are written across the scene's clip as it stands at that moment, and two later actions move that clip: fitting a scene to its narration (#121) and swapping in a replacement visual (#123). The captions stayed put — bunched into the first seconds of a stretched scene, or hanging over the scene after a shortened one — and re-running "Write captions" skips captioned scenes, so there was no way back. The caption panel now lists the scenes whose captions no longer span them and stretches each set back across its scene, keeping order and relative widths, with the last caption ending exactly where the scene does. The text is never touched, a scene too short to read its captions in (under 300ms each) is reported instead, a locked caption track is refused by name, and one safety version covers the batch. Rehearsed in a real browser in the order that causes the drift: three captions written across a 2.5s scene stayed at 0–2.5s after the scene grew to 12.73s, then realigned to 0–5.39/5.39–8.89/8.89–12.73s — the same 42/27/30 split, ending exactly on the scene, with the caption texts byte-identical before and after. Final gate: 203/203 Vitest + 66/66 native worker tests + build + worker syntax green.
- **PR #127** — real In/Out range export. Main `a08fc21d1817f5e7ce1312d534d49afe5aa62f75`. Render exposes explicit millisecond-accurate In/Out controls, a Whole timeline reset and a visible reason for every invalid selection. The pure range planner drops outside clips, moves retained clips relative to In, trims overlaps, advances source offsets by removed timeline time multiplied by clip speed, removes dissolves/fades whose edge was cut, and lets still-motion frame counts derive from each new clip duration. The timeline remains untouched and partial exports receive a safe range-specific filename. The executing FFmpeg test rendered a measured 1.0-second result and RGB-sampled the green second of a red/green/blue source after a 1.0-second source offset. Final gate: 208/208 Vitest + 67/67 native worker tests + production build + worker syntax green.
- **PR #129** — deterministic music ducking under speech. Main `19a1aab0fbed95e40532a574c4b0831fecdfe30b`. Music is lowered only across known voice/dialog intervals using deterministic FFmpeg volume automation rather than signal-dependent compression; expanded intervals are merged first so overlapping speech never compounds attenuation. Render exposes on/off, reduction, attack and release with validated defaults of 12 dB, 150 ms and 400 ms. Both compositor implementations contain byte-identical automation logic guarded by a drift test. A real render using constant music plus a silent voice carrier measured −21.1 dB outside speech and −33.1 dB during it: exactly the configured 12.0 dB reduction. Final gate: 211/211 Vitest + 69/69 native worker tests + production build + worker syntax green.
- **PR #131** — optional master export loudness. Main `a3de00121200384f675e85808f148dbd91b3de63`. Render exposes a clear opt-in master-normalization switch, off by default because it changes the sound. When selected, both compositor implementations apply the same validated FFmpeg loudnorm target (−14 LUFS, true peak no higher than −1.5 dBTP, loudness range 11 LU); the drift gate compares the complete master-filter expression. A real quiet-tone export measured −55.1 dB mean/−51.4 dB peak without processing and −13.3 dB mean/−9.6 dB peak with normalization. Final gate: 214/214 Vitest + 71/71 native worker tests + production build + worker syntax green.
- **PR #133** — reviewable storyboard scene ranges for Long-to-Short. Main `2276c1ee62afae5629aaea79aae728d05fedacd7`. The pure planner derives candidates only from storyboard scenes anchored to active visual timeline clips, includes cross-dissolve overlaps, groups contiguous scenes without exceeding 60 seconds and splits at real gaps. Missing or individually overlong scenes remain visible with exact reasons. Selecting a candidate fills the existing real In/Out controls without mutating the timeline or pretending that a heuristic is an AI editorial decision. Final gate: 217/217 Vitest + 71/71 native worker tests + production build + worker syntax green.
- **PR #135** — stable reviewed Short export identity. Main `f8f94aa7aadc35d01b0b8e6abcb88bafce1a03d8`. Selecting a reviewed Short candidate now remains visibly selected and gives the real export a safe, stable filename variant derived from its scene titles and exact In/Out range. Manual In/Out edits and Whole timeline reset clear the candidate identity, so an export filename can never claim a stale reviewed selection. Final gate: 218/218 Vitest + 71/71 native worker tests + production build + worker syntax green.
- **PR #137** — sequential batch export for reviewed Shorts. Main `939dd60dd9602df4ddd2625cdc6ab5bdf033e510`. Render can explicitly select any set of current scene-range candidates, prepare each through the existing real range-render path and export them one at a time so local FFmpeg work remains bounded. Every item keeps its stable scene/range/format-specific managed filename and the chosen ducking, loudness and target-format settings; progress, result, size and per-item failure remain visible. Cancellation stops the active worker job and marks work that was not submitted yet as cancelled, including the submit/cancel race. Candidate changes clear the selection so an unreviewed changed range cannot silently enter the batch. Final gate: 223/223 Vitest + 71/71 native worker tests + production build + worker syntax green.
- **PR #139** — persistent configurable Short duration. Main `30d95fa25106a986ffb21b39d863fdf4eee26518`. The scene-range planner is no longer fixed to 60 seconds: Render exposes 15/30/60/90-second presets plus a validated custom 1–600-second maximum, persists the choice as project metadata and immediately rebuilds the real scene groups. Older projects and malformed metadata safely fall back to 60 seconds. Replanning changes candidate identities and therefore clears any earlier single/batch selection before it could export an unreviewed changed range. Final gate: 225/225 Vitest + 71/71 native worker tests + production build + worker syntax green.
- **PR #141** — exact composed preview for reviewed Short ranges. Main `24555fa8810e13657bd585e590a9baa4bf14acbd`. A selected candidate can now be rendered and scrubbed through the real managed 540p preview path before export, using its exact In/Out trim and the current format, layers, captions, transforms, transitions, retiming, ducking and loudness settings. Preview MP4s remain below `KINAOU/Cache/Previews`, load only through authenticated media transport, support cancellation and are discarded from the UI when the candidate or settings change. The shared range planner preserves export/preview purpose and enforces their separate `Renders` vs `Cache/Previews` destinations. The executing worker test rendered both forms from a red/green/blue source, measured each at 1.0 seconds, sampled the green interval and authenticated-streamed the preview MP4. Final gate: 227/227 Vitest + 71/71 native worker tests + production build + worker syntax green.
- **PR #143** — persistent successful export history. Main `a6ed80851b61ca7c8face765d49bef70605a40b7`. Render now records validated project-level receipts only after successful worker completion for both individual and sequential Short exports. Each receipt retains the managed MP4 path, format, exact range, scene provenance, duration, optional size and completion time; malformed or duplicate stored entries are ignored and retention is capped at the newest 50. The visible Render history survives project serialization, while its explicit forget action removes metadata only and never deletes the MP4 below `KINAOU/Renders`. Final gate: 230/230 Vitest + 71/71 native worker tests + production build + worker syntax green.
- **PR #145** — real file availability for recorded exports. Main `51cee6a7adfa3090881349fcfae678412c4b0939`. Render can now explicitly check every retained export receipt against the connected managed drive and shows `FILE AVAILABLE` or `FILE MISSING` per MP4 plus exact totals and check time. The browser accepts at most 50 canonical `KINAOU/Renders/*.mp4` paths, rejects traversal and incomplete/unexpected worker responses, and keeps the result transient so a reconnected drive can be checked again. The worker performs real non-empty-file checks only; no receipt or file is changed. The executing test created one real render file and proved the present/missing distinction. Final gate: 231/231 Vitest + 71/71 native worker tests + production build + worker syntax green.
- **PR #147** — sequential multi-format Short adaptation. Main `e7dedeec44b1d71e9f1a86a9a51f907bbd51b66f`. For any reviewed Short candidate selection, Render now accepts one or more independent Landscape, Vertical and Square batch formats, expands the selection into unique candidate-by-format jobs and uses each format's real full-quality render profile and managed scene/range-specific filename. The project's main format and reviewed preview remain unchanged, each variant reports its own progress/result and successful receipt, and the existing queue still permits only one local FFmpeg render at a time. No platform or cloud API is involved. The executing compositor gate rendered the same managed source into non-empty 1920×1080, 1080×1920 and 1080×1080 MP4s. Final gate: 232/232 Vitest + 72/72 native worker tests + production build + worker syntax green.
- **PR #149** — independent multi-format Short preview. Main `664e1701eecff70f835d6874bcca37c57695afb6`. A selected reviewed Short can now be previewed as Landscape, Vertical or Square without changing the project's main format, so every batch adaptation can be inspected through the real compositor before export. Each choice uses its format-specific preview dimensions and managed cache path; changing format or render/audio inputs invalidates the stale preview. The executing worker test rendered the exact selected source interval as a 720×720 Square MP4, measured its duration and dimensions, sampled the expected frame content and fetched it through authenticated preview transport. Final gate: 232/232 Vitest + 72/72 native worker tests + production build + worker syntax green.
- **PR #151** — real local Publish handoff packages. Main `811b21a5930d722985828575d4dc802082b42488`. The formerly reserved Publish area now turns a selected successful export plus reviewed destination profile, title, description and tags into a new attributable `*.publish.json` sidecar beside its MP4. The authenticated worker revalidates the exact export receipt, rejects unsafe paths and missing/empty media, records the actual file size and server time, and uses a unique non-overwriting filename. It never modifies, moves, deletes or uploads the MP4; YouTube, Instagram and TikTok are metadata profiles only, with no provider API or credentials. The executing test wrote two distinct packages for one real managed file, read back the complete JSON, proved the source bytes unchanged and rejected missing media, path escape, duplicate tags and bad authentication. Final gate: 236/236 Vitest + 73/73 native worker tests + production build + worker syntax green.
- **PR #153** — reopenable local Publish package library. Main `00783280c4b76261e9801dbf74ff7f1b05ffde39`. Publish can now explicitly refresh up to 200 earlier handoff sidecars for the active project, see whether each referenced MP4 still exists, and reopen its reviewed destination/title/description/tags; when the exact export receipt is still in project history it is selected too, otherwise the current export remains selected and the limitation is stated. The authenticated worker scans only a bounded depth/count below `KINAOU/Renders`, validates every document again, excludes other project IDs plus malformed/oversized files, skips symlinks and never mutates media or sidecars. A separate capability makes an older worker ask for a restart instead of pretending the library exists. Executing tests created multiple real sidecars, excluded another project and malformed/symlinked entries, reported present then missing source media, and rejected invalid project IDs plus bad auth. Final gate: 238/238 Vitest + 73/73 native worker tests + production build + worker syntax green.
- **PR #155** — reusable project Publish defaults. Main `3d5690dfe2b9fcc163c5050e96824b3d8a35787d`. Publish can explicitly save the current destination profile, title, description and normalized tags with the active project, load them again on demand, restore them automatically when that project is reopened, or clear the saved copy without changing the current form. The versioned bounded schema lives in project metadata, survives serialization/backups through the existing project contract, stays isolated from other projects, ignores malformed stored values and treats an identical save as a no-op. These are form defaults only: no worker request, sidecar, upload, credential or provider API is triggered by saving/loading them. Final gate: 240/240 Vitest + 73/73 native worker tests + production build + worker syntax green.
- **PR #157** — persistent per-format reframing. Main `bf2e8e12478222b79d7f0e193d1b848671961440`. Landscape, Vertical and Square now each retain an independent whole-image/fill-frame choice plus normalized horizontal and vertical crop focus in project metadata. Timeline preview, reviewed Short preview, full export and sequential multi-format batches all compile through the same format preset; changing framing invalidates stale Short preview state. Off-centre jobs declare a dedicated worker capability so an older running worker asks for a restart instead of silently centre-cropping. Both compositor implementations validate and apply identical crop expressions, and an executing FFmpeg test rendered a red/green/blue source twice to prove that left focus selects the red region while right focus selects blue. Final gate: 242/242 Vitest + 74/74 native worker tests + production build + worker syntax green.
- **PR #159** — real local Publish preflight. Main `a77bb8563d27c39ee3655d0dfdf1bf88a9c3ca8b`. A selected successful export must now pass a visible authenticated ffprobe check before its local handoff can be created: actual byte size, a real video stream, full-quality format dimensions and duration within a 250ms container tolerance are compared with the retained receipt; audio presence is reported but a deliberately silent video remains valid. Package creation reruns the same checks immediately, so a mismatched format or file changed after review is blocked before any sidecar is written. `publish-preflight` and `publish-package` are advertised only with ffprobe available. The browser empty-state test found and fixed a null-result crash, then verified both actions disabled and zero fresh console errors. The executing gate generated a real 1920×1080 H.264/AAC MP4, passed its correct receipt, rejected a false Vertical receipt, detected a post-review byte change and proved successful packaging left the MP4 byte-identical. Final gate: 245/245 Vitest + 74/74 native worker tests + production build + worker syntax green.
- **PR #161** — durable local Publish package integrity. Main `3ba15f680169754bb5b962fd45f6b07a8abe6fd4`. New handoffs are backward-compatible version-2 sidecars retaining the real preflight media facts and a constant-memory streaming SHA-256 fingerprint of the selected MP4. The bounded authenticated integrity endpoint opens one selected sidecar and source without following symlinks and reports `unchanged`, `modified` (including same-byte-length changes), or `missing`; legacy version-1 packages remain readable and honestly report `unverifiable`. Library refresh deliberately never hashes the full video collection, while the Publish UI shows unverified/verified state and requires the new worker capability before creating a v2 package. The executing gate generated a real H.264/AAC MP4, matched its digest, detected a same-size byte mutation, confirmed restoration, accepted a legacy package, reported a removed source and rejected traversal/bad auth without altering the video. Final gate: 247/247 Vitest + 74/74 native worker tests + production build + worker syntax green.
- **PR #163** — persistent resumable Short export batches. Main `4cdef7fcf49d3bc35bcddb86a4c0e24c01645b24`. A reviewed Short × format batch now lives as bounded, strictly validated project data with exact output identities, presets, audio settings, worker job state and terminal receipts. Reloading rebuilds only unfinished range plans and requires their canonical configuration signatures to match the current timeline/media/settings; completed/failed/cancelled entries are never silently resubmitted. If a restarted worker has forgotten an active job, KINAOU requeues it only when its planned output is absent; an existing partial or complete file is left untouched and reported instead. The worker additionally refuses any existing durable export path while still allowing deterministic cache previews to refresh. The executing FFmpeg gate rendered the exact range, rejected a duplicate export and proved its bytes unchanged, then refreshed the same preview path successfully. Final gate: 250/250 Vitest + 74/74 native worker tests + production build + worker syntax green.
- **PR #165** — selective non-overwriting Short batch recovery. Main `490c7dcfdc05f11907308fcca5ae36986804d56c`. Once a saved batch is fully terminal, Render exposes only the latest failed/cancelled attempt of each candidate × format for explicit selection. A retry must still match the current reviewed scene identity and exact range, rebuilds its real export plan from the current project/framing/audio settings, appends a validated attempt lineage and gets a fresh managed output path; successful files and every earlier attempt remain untouched. Queued retries persist and pass the same exact-signature reload gate, old pre-retry batches remain readable, malformed lineage and duplicate/stale/successful/nonterminal selections are rejected, and the combined attempt history stays bounded at 100. Final gate: 252/252 Vitest + 74/74 native worker tests + production build + worker syntax green.
- **PR #167** — bounded project-local Short batch archive. Main `17e8a24f744946dc9d0e52964282aeebc0ba84e2`. Before a fully terminal queue is replaced or explicitly cleared, Render now stores a compact immutable summary with its preparation/completion/archive times plus every attempt's candidate, format, exact range, terminal state, attempt number, managed output path and successful byte size. The newest ten runs of at most 100 attempts each survive project serialization and appear as expandable Past Short Batches; unfinished batches cannot enter the archive, repeated batch IDs are deduplicated, malformed stored entries are skipped and retention drops only the oldest summary. Render plans, timeline payloads and errors are deliberately not duplicated. Forgetting one summary removes only project metadata and never checks, moves, changes or deletes an MP4. Final gate: 254/254 Vitest + 74/74 native worker tests + production build + worker syntax green.
- **PR #169** — on-demand archived Short output availability. Main `a5e9d76533534e00ac792eee39cd2763c8c67cd9`. Render can now check exactly one archived batch against the connected managed drive and shows aggregate plus per-attempt present/missing results with a check timestamp. All recorded paths are canonical managed `KINAOU/Renders/*.mp4` paths validated before the first request; a batch of up to 100 attempts is split into the existing authenticated read-only endpoint's 50-path limit. Results are deliberately ephemeral presence observations rather than integrity claims: no file is hashed, moved, changed or deleted, and the archive itself is not rewritten. Final gate: 255/255 Vitest + 74/74 native worker tests + production build + worker syntax green.
- **PR #171** — review-driven archived Short selection reuse. Main `bc8d746cb74799cdbdc710ca3d2f8be2a4d65c5c`. An archived terminal batch can now repopulate the normal Short review controls with its complete candidate × format selection, but it never starts a render or changes archive/media data. Only candidates that remain currently exportable are selected; unavailable archived candidates are named. Incomplete historical selection matrices are rejected rather than expanded into combinations the user never chose. The user reviews the current ranges and presses the existing explicit export action; that creates fresh output identities, leaving all archived MP4s untouched. Final gate: 256/256 Vitest + 74/74 native worker tests + production build + worker syntax green.
- **PR #173** — named project-local Short recipes. Main `b04de6fe60b0a93664f4d5e4e91e9af3a0c197a6`. Render now saves up to ten uniquely named recipes containing only selected current candidate identities and output formats. They store no render plan, job, output path or media reference. Reopening a recipe merely restores currently available choices to the visible review controls and names any unavailable candidates; it cannot start a render. An explicit normal export remains required and creates fresh output identities. Malformed/duplicate persisted recipes are ignored, and forgetting a recipe removes only its project metadata. Final gate: 258/258 Vitest + 74/74 native worker tests + production build + worker syntax green.
- **PR #175** — multilingual project content profile. Main `0f5049ed812e4154b8f9549bc64c50595108ac4b`. Director now exposes one persistent profile for source language, German/English/French output, `WORLD` or a two-letter target market, intended audience, conversion objective and voice/tone. The form is bounded, validated and reviewable before save. Local Ollama Director generation receives an explicit deterministic profile block requiring every user-facing plan field in the chosen output language while preserving source facts and forbidding invented current-trend claims. Existing projects receive safe defaults; no runtime, model, cloud service or download is implied. Final gate: 260/260 Vitest + 74/74 native worker tests + production build + worker syntax green.
- **PR #123** — a replaced scene visual replaces its clip. Main `da0837be1fc4b11c95fe2feff5fa781abcaaa20a`. Assembly recognised a scene by its asset, so replacing a scene's visual afterwards — exactly what the fulfillment panel invites — made the new media look like an unplaced scene: the next assembly appended it at the end and left the old picture in the scene's slot, showing both. A clip now records which scene put it there, so a repeat is the same scene rather than the same file, and Studio offers to swap the media inside the clip that already exists — same start, same length, so narration, captions and dissolves around it stay valid. The swap drops motion when the replacement is footage, resets retiming when it is a still, fills only as much as the footage really has (in timeline milliseconds, so a retimed clip counts), keeps a scene stretched to its narration at that longer length, trims or drops a dissolve that no longer fits, and refuses a locked track by name instead of half-applying. Two consequences of the same change: a storyboard may now show one picture in two scenes (captions, narration and the fit look for the clip stamped with their own scene), and a clip placed before scenes were recorded is adopted the next time assembly runs. Proven in a real browser: two assembled scenes, scene one replaced, one click, and the rendered MP4 shows the replacement at 1.0s (RGB 229,165,9) with the second scene untouched at 4.5s (RGB 27,113,216) — re-assembling appended nothing. Final gate: 193/193 Vitest + 66/66 native worker tests + build + worker syntax green.
- **PR #121** — the picture adapts to the voice. Main `5640b2e17423219c9f61ce9edf1b4662f8cd160c`. Narration kept its natural length, so a scene shorter than its voice rendered a black frame with someone still talking over it. Studio now lists exactly those scenes and offers to fit them: each overrunning visual grows until it covers its own narration, and everything sitting in a later scene moves by the same amount, so nothing is overwritten and the cross-dissolve overlaps keep their width (a clip inside the extended scene keeps its place, because the time is added behind it). The voice is never shortened, footage is never stretched past its last frame — a video is extended only as far as its source allows, measured in timeline milliseconds so a retimed clip counts correctly, and unknown source length means no extension rather than frozen frames — and a locked track that would have to move refuses the whole operation instead of half-applying it. One safety version per batch. Proven by rendering both timelines: before the fit 5.87s with a black frame at 5.5s (luma 0.0), after the fit 8.10s with the picture at 1.0s, 5.5s and 7.9s (luma ~110) and identical narration level at the tail (mean -21.1 dB). Final gate: 177/177 Vitest + 66/66 native worker tests + build + worker syntax green.
- **PR #119** — the app has an icon instead of a 404. Main `ce735d9a4462e734da1f744741b8f93957808761`. A console-error sweep across all twelve app sections found exactly one problem: every page load requested `/favicon.ico` and got a 404, because no icon was ever declared. `index.html` now carries an inline SVG data-URI icon, so the app ships no external icon file, makes no extra network request, and the browser console is clean on every section. Verified by reloading each section in a real browser and asserting zero console errors. Final gate: 163/163 Vitest + 66/66 native worker tests + build + worker syntax green.
- **PR #117** — every scene speaks with the local voice. Main `b034650f6de080dda9fd17f3fe1bd7713ec6fbdc`. Piper could already speak arbitrary text, but nothing connected the Director's script to the scenes it belongs to. Studio's Script→Narration panel speaks each scene description through the installed voice and places it under that scene on the voice track, timed from where the visual actually sits. Narration keeps its natural length — trimming a sentence to fit a picture would swallow words — so an overrun is reported with its exact overshoot instead. Jobs run one scene at a time with the project persisted after each, failed scenes report their own reason without stopping the rest, narrated scenes skip on re-runs, one safety version per batch. Verified end-to-end in a real browser using a stand-in for the Piper binary with the same argument contract. Final gate: 163/163 Vitest + 66/66 native worker tests + build + worker syntax green.
- **PR #116** — assembled scenes get motion and cross-dissolves by default. Main `b3c3de8631b54660cf0533820f74c13559ab51f9`. Assembly produced correct video that looked like a slide projector. Two visible opt-out options, on by default: alternating gentle motion on stills (never on video), and a cross-dissolve implemented by starting each scene half a second inside its predecessor with a dissolve-in, so both are on screen for the overlap — a real cross-fade rather than a fade through black, verified by rendering overlapping clips before the code was written (luma 0→41→142→220→255). The overlap is capped at the scene's own length; with both options off the placement is byte-for-byte what it was. Final gate: 155/155 Vitest + 66/66 native worker tests + build + worker syntax green.
- **PR #115** — slow motion on still scenes. Main `26ce54fa6cc2963d3ca38b355efa939219e01a9f`. A still clip can carry a zoom-in or zoom-out over its own length. The design problem was where the zoom renders: zoompan needs a fixed output size, and using the canvas would stretch a letterboxed still. So cover zooms into the canvas, contain computes the letterboxed size from the source pixels, unknown pixel dimensions render motionless rather than guessing, and motion on anything but a still is refused. Proven by rendering: a motionless still is identical between 0.2s and 2.7s while zoom-in/out visibly differ, and a 16:9 still in a square canvas keeps empty letterbox bands throughout — which a stretched frame would have filled. Final gate: 150/150 Vitest + 66/66 native worker tests + build + worker syntax green.
- **PR #113** — the edit semantics are executed, not spelled. Main `5ab070a95b883681b867a34c3b174c52724336a8`. Four executing checks designed so a wrong implementation cannot pass by accident: a source that is black for two seconds then white proves retiming (a 2× clip is already in the white half where a 1× clip is not), a 1s fade-in must be dark at 0.1s and bright at 1.5s, scale 0.4 must leave the corner showing empty canvas, and gain 0.25 measured with `volumedetect` must be ≥9 dB below gain 1. All pass against FFmpeg 6.1.1. Final gate: 142/142 Vitest + 59/59 native worker tests + build + worker syntax green.
- **PR #112** — the FFmpeg compositor is finally executed by tests. Main `d110a14e49b6021b8b9ad0ab39f57c5c95fe25af`. Every render test so far compared filter strings; nothing ever ran FFmpeg, so a plausible but wrong filter graph would pass the whole gate and fail only on the user's Mac. The smoke test boots the real worker against a temp managed root, generates its own sources and renders four real MP4s through the actual job pipeline, then measures pixels: landscape keeps 1920×1080; vertical `cover` fills the frame while `contain` letterboxes it (the first executing proof that PR #109 is correct); captions are genuinely burnt in (a flat dark reference render has no bright pixels in the caption band, the captioned one does). CI now installs FFmpeg so it runs there; without FFmpeg the test skips with a clear message. Two measurement traps are documented in the code because both made a working feature look broken: a busy test pattern hides caption text among its own bright pixels, and downscaling a region before counting averages thin white strokes away. Final gate: 142/142 Vitest + 55/55 native worker tests + build + worker syntax green.
- **PR #110** — captions written from the scene descriptions. Main `8ff01616824daee23e53d47ad2f09faaaa95762c`. A rendered scene sequence had no words unless every caption was typed by hand, while the Director's script sat unused. Studio's Script→Captions panel splits each scene description at sentence boundaries (word boundaries for one over-long sentence) and gives each chunk a length-weighted share of the clip's time — taken from where the visual actually sits on the timeline, not from planned durations. A clip too short for every chunk merges the overflow into the last caption rather than dropping text. Re-runnable (captioned scenes skipped), per-scene skip reasons, captions stay editable in the Caption Studio, safety version only on real changes. No AI runtime involved. Verified end-to-end in a real browser (blocked → assemble → 2 sentence-split captions at 0–2.9s/2.9–8.0s → re-run without duplicates). Final gate: 142/142 Vitest + 51/51 native worker tests + build + worker syntax green.
- **PR #109** — vertical and square platform formats as a project property. Main `a3f511bfa68e5e1d3ccd6dd4f05b4dba9d3345bb`. Export was hardwired to 1920×1080, leaving the Adapt step of the long-term flow unimplemented. A project now carries a target format (landscape 16:9, vertical 9:16, square 1:1) chosen in the Render panel and persisted in metadata; because it belongs to the project, the composed preview switches with it, so a vertical short is edited in a vertical preview. Render presets gained a fit mode: `contain` (default, unchanged behaviour) letterboxes, `cover` fills the canvas via `force_original_aspect_ratio=increase` plus a centre crop — what wide material needs as a short. Both compositors derive the filter identically (guarded by a drift test), the worker validates the mode, and outputs carry the format in their filename. Final gate: 131/131 Vitest + 51/51 native worker tests + build + worker syntax green.
- **PR #107** — assemble the timeline from fulfilled storyboard scenes. Main `b8f96efafa76001739b842a744f756f97204deb7`. The autonomous loop filled scenes with real material, but building the video still meant placing every scene by hand — the last tedious gap in Discover→Direct→Generate→Edit→Export. Studio's new Storyboard→Timeline panel appends every fulfilled scene to a chosen visual track in storyboard order, gapless after existing content: stills hold for exactly their scene duration, videos are capped at the footage that exists (and say so). Existing clips are never moved, scenes already on the track are skipped so re-runs never duplicate, and unfulfilled/offline/unmanaged/missing/non-visual scenes are reported per scene instead of aborting the run. The disabled state names what is missing. Assembly is a pure function, so the safety version is written only when clips are actually placed; restoring it undoes the whole assembly. Verified end-to-end in a real browser (blocked state → capture → fulfil → assemble at scene duration → re-run skips → safety version holds the empty track). Final gate: 126/126 Vitest + 49/49 native worker tests + build + worker syntax green.
- **PR #104** — project backups on the managed drive. Main `f43f28ad968433cac4d1f2142e46f4e9a52a769c`. Projects lived only in browser storage, so clearing site data would silently destroy them and the milestone's "projects on the external SSD" was unmet. The worker gains `POST /projects/save` (atomic pretty-JSON write to `KINAOU/Projects/<id>.json`, 5 MB bound, strict id/title/updatedAt validation, ids locked to `[A-Za-z0-9-]`), `GET /projects/backups` (bounded listing with title/updatedAt/size/modified, unparseable files skipped) and `POST /projects/restore`. The Projects section's new Drive Backups panel backs up the open project in one click, lists the drive, and restores any backup — validated through the full project schema, with an automatic "Before drive restore" safety version when it replaces an existing browser copy. Verified end-to-end in a real browser (save → file on drive → list → restore reopens the project). Final gate: 120/120 Vitest + 49/49 native worker tests + build + worker syntax green.
- **PR #102** — speed buttons state operation and target. Main `ffed6369f13619190b25cc824ccf2488b95f0032`. Real-Mac finding: "Speed 2×" read as "set to 2×" but doubles the current speed (0.5× → 1.00×), so the tester expected fast motion and got normal speed. Buttons now read "Halve speed → 0.50×" / "Double speed → 1.00×" with the computed target and tooltips; the #96 source-bounds guard and Speed reset unchanged. Final gate: 119/119 Vitest + 48/48 native worker tests + build + worker syntax green.
- **PR #101** — strict dev-server port and visible build identity. Main `a81549e7cc3ee1f9e158bdc20d0af3834d971e73`. Root cause of the "Add to timeline not clickable" mystery: a zombie dev server held port 4173 for hours, new `npm run dev` runs silently fell back to other ports, and the browser kept loading stale code — merged UI fixes never became visible. A clean-browser end-to-end run (create project → connect worker → real web capture → Add to timeline) proved the placement path sound. Hardening: vite `strictPort` (dev and preview fail loudly when 4173 is taken — project data is origin-bound to localhost:4173), a sidebar "build <commit> · served since <time>" identity via vite define + git, MAC-TEST troubleshooting rows, `@types/node` dev dependency. Final gate: 119/119 Vitest + 48/48 native worker tests + build + worker syntax green.
- **PR #100** — honest media availability for drive disconnect/reconnect. Main `ff3049e4ac99dd6fdf8e4f5710fb115231df183f`. The core milestone's disconnect/reconnect resilience existed only as an unused flag: nothing ever set `offline`. The worker gains bounded `POST /assets/availability` (per-path real file presence under the managed root, auth + path-escape enforced), the client parses strictly, and a new Assets panel checks every managed asset in one click — missing media goes OFFLINE (already excluded from rendering and placement), found media comes back online, files are never touched, and an honest summary reports the counts. Verified end-to-end in a real browser: capture → file moved away → check marks OFFLINE with the placement reason shown → file back → check restores AVAILABLE. Final gate: 119/119 Vitest + 48/48 native worker tests + build + worker syntax green.
- **PR #99** — a disabled Add to timeline button states its reason. Main `08c1c5b72b05b7d8d95247c0ad195ac132a6c61d`. Second occurrence of an unexplained grey placement button; the control now derives the exact disable cause (offline asset, unmanaged asset, locked target track incl. which one) and renders it under the button. Disable conditions unchanged; #94's click-error display remains for runtime failures. Final gate: 114/114 Vitest + 47/47 native worker tests + build + worker syntax green.
- **PR #96** — over-retimed clips explain themselves instead of crashing the app. Main `28a4047e878326340020b6a5349392b51ca7803f`. Real-Mac finding (severe): Speed 2× on a clip already using nearly its whole source made the entire app permanently dark — the composed preview builds its render plan in `useMemo`, `createRenderPlan` throws "Retimed source range exceeds asset duration" (timeline duration stays fixed on speed changes per PR #23), a render-time exception unmounts the React tree, and the persisted state re-crashed every reload. Layered fix: `clipSpeedFitsSource` in the timeline core, `renderReadiness` reports the case as a clear not-ready reason for preview and export, the preview plan memo is try/catch-wrapped, the Speed 2× button disables with a tooltip when doubling would exceed the source, and a new app-level error boundary turns any future render error into an honest recoverable screen instead of a dark page. Affected persisted projects load again and recover via Speed reset — no data loss. Final gate: 114/114 Vitest + 47/47 native worker tests + production build + worker syntax green.
- **PR #94** — timeline placement failures visible instead of swallowed. Main `44827651774293f842377eae1dddf7124e76e849`. Real-Mac finding: clicking "Add to timeline" could silently do nothing because `placeAssetOnTrack`'s honest errors (missing media duration, locked track, incompatible kind) were neither caught nor displayed by the placement control. The control now shows the actual message inline under the button; retrying or switching the target track clears it. Placement logic unchanged and already covered by existing tests. Final gate: 111/111 Vitest + 47/47 native worker tests + production build + worker syntax green.
- **PR #92** — occupied worker port explained instead of a crash. Main `085bd96e0a8b37c10105912fad2bf0906ca95e3c`. Real-Mac finding: starting the worker while an earlier instance still held 127.0.0.1:43117 died with an unhandled EADDRINUSE stack trace (hit three times during the live test). The server now catches that case, explains that an earlier worker is probably still running, prints the exact `kill $(lsof -ti :43117)` remedy plus the `KINAOU_WORKER_PORT` alternative, and exits cleanly; other server errors still throw. Regression test boots two real workers on one port and asserts the clean exit, guidance text and absence of the stack trace. Final gate: 111/111 Vitest + 47/47 native worker tests + production build + worker syntax green.
- **PR #90** — FFmpeg-backed capabilities advertised only when FFmpeg exists. Main `9329355f7b7ba8ebb1f8fc603f7402b646e1693f`. Real-Mac finding (Stage 2): on a Mac without FFmpeg the health handshake still hardcoded the `ffmpeg`, `media-probe`, `media-proxy`, `media-thumbnail` and `media-waveform` chips, so the UI looked ready while the first import honestly failed with "ffprobe is not available". The chips now follow the actual ffmpeg/ffprobe detection (`filesystem`/`asset-upload` stay unconditional because they genuinely work without FFmpeg), existing UI gating then shows the true state, and the worker prints a startup warning naming `brew install ffmpeg` and the required restart. New regression tests boot the real worker process with an empty PATH and with stub binaries and assert chips, versions and warning. Final gate: 111/111 Vitest + 46/46 native worker tests + production build + worker syntax green.
- **PR #88** — web captures complete from the produced screenshot, and job polling survives failed requests. Main `291ea81d79fbed1e9fffba91f1608c9324ecd068`. Real-Mac finding: headless browsers can keep running long after writing the screenshot, so jobs hung at 10% until the timeout, whose cleanup then deleted the already-finished PNG. The worker now watches the target file during a web capture and completes the job once the screenshot exists and its size is stable across polls, terminating the lingering browser itself; real crashes still fail via the exit code, and timeout/cancel cleanup is unchanged. Verified end-to-end against both a fast-exiting and a deliberately lingering browser. Second fix: a single failed status request no longer silently killed the PWA poll loop — all six job panels (image, video, STT, TTS, capture, web capture) now surface the error and re-arm polling. Final gate: 111/111 Vitest + 44/44 native worker tests + production build + worker syntax green.
- **PR #87** — throwaway browser profiles moved off the managed drive. Main `ce7ea095886329956251652c248cf979b24a7991`. Real-Mac finding: the user's SSD is exFAT, where the symlink-based profile locks of Chromium/Firefox fail, so every web capture timed out. The one-job temporary browser profile now lives in the OS temp directory instead of `KINAOU/Temp` — a deliberate, disclosed exception to the managed-root rule covering only browser-internal scratch data that is created and removed per job; every KINAOU output (temp screenshot and final asset) stays under the managed root. Final gate: 110/110 Vitest + 44/44 native worker tests + production build + worker syntax green.
- **PR #86** — WorkerClient works in real browsers again. Main `df85f01f0cad5e6d99e107a683fa1d86c570aca8`. Real-Mac finding: every PWA worker call failed with `Can only call Window.fetch on instances of Window` because the client stored the global `fetch` unbound — invisible in Node-based tests, fatal in Chromium. The default fetch implementation is now an arrow wrapper, and a regression test asserts global fetch is never invoked bound to the client instance. Final gate: 110/110 Vitest + 44/44 native worker tests + production build + worker syntax green.
- **PR #83** — editable plan review as second media acquisition mode. Main `17cbefc3ef829df804eee7b41d8011be5c2d3f7f`. Alongside the automatic run, "Propose plan for review" delivers the plan first as an editable list: per item the scene, action and its URL/app name/prompt can be changed, items removed or added; "Validate & run plan" re-validates the edited plan against the same strict rules (existing scenes, credential-free http(s) URLs, injection-safe app names, per-item errors) before executing. The durable run record notes auto vs reviewed; both modes share the pre-run safety version and fill-only-empty-scenes. Final gate: 110/110 Vitest + 44/44 native worker tests + production build + worker syntax green.
- **PR #81** — media plan runs automatically, review moves to the results. Main `f9cf5af7ca7bdb9a250eace3d22d6a999d53b558`. Per explicit product decision the pre-approval step from PR #79 was removed: one action lets the local model decide per scene and KINAOU acquires everything immediately. Control lives on the result side: an automatic Version History snapshot before the run undoes the whole acquisition in one restore, the item list doubles as a live result review, a durable run record (plan + per-item outcomes) is stored in project metadata, only still-empty scenes are filled, and URLs/app names stay strictly validated. Final gate: 110/110 Vitest + 44/44 native worker tests + production build + worker syntax green.
- **PR #79** — autonomous scene media acquisition plan with one-review run. Main `35aab71779d22c3a08636c694994e2f6c9a561ed`. The local Ollama model proposes per storyboard scene how to obtain its visual — a concrete public URL to web-capture, an exact app name to window-capture, or a generation prompt — as a bounded schema whose scene references, credential-free http(s) URLs and injection-safe app names are re-validated client-side. The user reviews once with per-item selection (LLM-proposed fetches and app activations never run unseen); approved items then run fully automatically: capture or generate, register with provenance, and assign only still-empty scenes. Fulfilled scenes keep their visual, new material stays as an alternative, failed items report per-item errors without stopping the rest. Final gate: 110/110 Vitest + 44/44 native worker tests + production build + worker syntax green.
- **PR #77** — automated app-window capture by app name. Main `e705929a1390191df93f291565c4b2f3652946d5`. A named app is activated (launched if needed) through osascript with the name passed strictly as argv — never interpolated into the AppleScript — its front window bounds are read via System Events (negative multi-display coordinates supported), and exactly that region is captured as screenshot or bounded stoppable recording. Resolved bounds and app name are recorded in the real-capture provenance; denied Automation/Accessibility permission and missing windows surface as honest dedicated errors. Final gate: 108/108 Vitest + 44/44 native worker tests + production build + worker syntax green.
- **PR #76** — real reproducible website captures through installed local browsers. Main `dd736c90e838511a50cfd18b8edb804871a37a96`. Discovery finds already-installed Chrome/Chromium/Firefox (env-overridable, nothing downloaded, real versions reported); `web-capture` is advertised only when one exists. Cancellable jobs render exactly the explicitly entered credential-free http(s) URL headless in an isolated one-job temporary profile (always removed) with bounded viewport and timeout, land the PNG atomically in `KINAOU/Assets/WebCaptures`, and register once with real-capture provenance carrying URL, browser, version and viewport — regenerable anytime, distinct from generated visuals and screen captures. The chromium command was verified end-to-end against a live local page. Final gate: 108/108 Vitest + 41/41 native worker tests + production build + worker syntax green.
- **PR #74** — interactive on-screen selection for screenshots. Main `7da21ebdbe0513b2cc84d5e0f58f79e74f4e91c8`. Screenshots can use screencapture's interactive mode: the user's own cursor selects an area or, via Space, a specific window directly on the real screen — the most explicit capture authorization and per-window capture without any window enumeration. Interactive is screenshots-only, mutually exclusive with fixed display/region/delay, recorded in the real-capture provenance, and an on-screen Escape cancels with a dedicated honest error. The Capture panel offers the target choice and explains the crosshair flow. Final gate: 106/106 Vitest + 36/36 native worker tests + production build + worker syntax green.
- **PR #72** — visible Capture Studio registering real screen captures as assets. Main `a8cb091c5b549bced2044043f0eb994e8ed0d707`. Capture is a functional section with explicit authorization UX: it states plainly that it records the actual screen, every capture starts only on an explicit click, availability is honest, screenshots support a bring-window-to-front delay, and recordings offer Stop-and-keep or Discard. Each success registers exactly once as a managed image (`image/png`) or video (`video/quicktime`, with real duration) asset carrying `captured: true` real-capture provenance and never a generated flag; captured media lists as REAL CAPTURE and reaches scenes/timeline only through the existing explicit controls. Final gate: 106/106 Vitest + 36/36 native worker tests + production build + worker syntax green.
- **PR #71** — explicit real screen capture jobs via macOS screencapture. Main `849f41c22a1cf60289f584a6b570b38cd2a1cab9`. The worker advertises `screen-capture` only on macOS with the built-in `/usr/sbin/screencapture` present and runs explicit user-initiated jobs, never AI-started: bounded validated screenshots (0–10s delay, display 1–16 or integer pixel region, forced PNG) and 1–600s recordings that can be stopped early (SIGINT keeps the recorded part) or cancelled (discards). Commands are shell-free, output goes through `KINAOU/Temp/Captures` into `KINAOU/Assets/Captures` atomically, recordings are ffprobe-probed for real duration, failures/cancels remove partial data, and every job carries `real-capture` provenance so captures can never be confused with generated visuals. Missing macOS Screen Recording permission surfaces as an honest error naming the system setting. Final gate: 104/104 Vitest + 36/36 native worker tests + production build + worker syntax green.
- **PR #69** — visible local Video Studio with attributable generated video assets. Main `075a46b15c60abd0dbfce266315f79904598d8ce`. Video is a functional section mirroring the Image Studio: honest availability for video-declaring managed templates, template-driven inputs, user-controlled seed, start/progress/cancel against `/video/jobs`, and once-only registration of each success as a managed video asset carrying real duration/dimensions plus full prompt/template/seed provenance. Clips are labelled GENERATED (never presented as real recordings), offer Reuse settings for explicit regeneration without overwriting anything, fulfil storyboard scenes through the shared reversible `SceneFulfillmentControl`, and reach the timeline only via the explicit placement control. Final gate: 100/100 Vitest + 32/32 native worker tests + production build + worker syntax green.
- **PR #67** — capability-based local video generation foundation. Main `0e4d897ad30bd7280c1759a67c59e09d81d2601c`. Managed ComfyUI workflow templates declare an explicit `mediaType` (image default, video); `image-generation` and `video-generation` are advertised independently per existing valid templates. Mirrored `/video/templates` and `/video/jobs` endpoints reuse the cancellable managed job lifecycle; history collection reads images/videos/gifs output lists and the job picks the output matching its declared media type. All downloads now stream through a byte limiter into managed temp `.part` files before atomic rename into `KINAOU/Assets/GeneratedImages` or `KINAOU/Assets/GeneratedVideo` (2 GB video bound); successful videos are ffprobe-probed so results carry real duration/dimensions, and provenance is stamped with the media type. No video UI yet. Final gate: 98/98 Vitest + 32/32 native worker tests + production build + worker syntax green.
- **PR #65** — explicit reversible storyboard scene fulfillment. Main `848ec85084b05dede299c95c7f15e8898a9a66fb`. Storyboard entries carry an optional fulfilling `assetId` (backward compatible). Assignment accepts only existing online image/video assets, same-asset re-assignment is a no-op, and replacing a fulfilled scene requires an explicit replace action that first stores an automatic Version History safety snapshot. Unassigned images remain available as alternatives, clearing a scene never deletes the asset, and the Image Studio fulfillment overview shows each scene's honest state including a missing assigned asset. Final gate: 93/93 Vitest + 30/30 native worker tests + production build + worker syntax green.
- **PR #63** — real Image Studio with attributable generated image assets. Main `fc7aeabd97b8949fa8d26f8591caaae1dff73173`. Images is now a functional section: honest availability (worker connection, ComfyUI reachability with version, count of valid managed templates, explicit message about what is missing), template-driven inputs (negative prompt and dimensions appear only when the template binds them), user-controlled seed with randomize, start/progress/cancel against the worker job endpoints. Each successful output is registered exactly once (idempotent by URI) as a managed image asset with durable structured provenance — generated flag, adapter, template id/path, seed, dimensions, prompts, job id, generation timestamp, MIME/size — labelled GENERATED (never a real screen capture), and placed only through the existing explicit `AssetPlacementControl`. Final gate: 89/89 Vitest + 30/30 native worker tests + production build + worker syntax green.
- **PR #61** — cancellable managed ComfyUI image generation jobs. Main `93554f67eaff3f79aa4906a4855b7a3f243a5de5`. The worker detects a locally installed ComfyUI through `/system_stats` on the normalized loopback endpoint only, lists size-bounded valid managed templates with honest availability, and runs real jobs: managed-set-validated template, bounded binding, submission to `/prompt`, history/queue polling with an overall timeout and coarse progress. Cancellation is immediate locally and best-effort remotely (own pending prompt deleted from the queue, own running prompt interrupted, foreign prompts untouched). Output images are downloaded size-bounded through a strictly validated `/view` query, written to managed temp `.part` files and atomically renamed into `KINAOU/Assets/GeneratedImages`; failure/cancel removes partial data. Trusted adapter/template/seed/dimension/prompt provenance is stamped server-side; `image-generation` is advertised only when ComfyUI and at least one valid template exist. The PWA WorkerClient validates availability, job state, provenance and managed result paths. No UI yet. Final gate: 87/87 Vitest + 30/30 native worker tests + production build + worker syntax green.

## CI incident record (2026-09-06)
The repeated GitHub “all jobs failed” emails did not indicate a broken `main`. CI was configured with `push.branches: ['**']`, so every intermediate commit on every feature branch immediately triggered a full run; opening a PR triggered another run for the same head. A PR #11 work-in-progress sequence produced 13 consecutive red push runs while native `node:test` coverage was temporarily being discovered by Vitest; the final feature head, PR gate, merge commit and documentation commit were green. Earlier isolated failures were normal pre-fix commits: the initial CSS side-effect import lacked Vite types, the Worker UI used nested probe fields not present in its type, and the compositor test still expected complex plans to be rejected after support was added. All were corrected before their PRs merged. There are no open feature PRs after PR #175.

## Important modules
- `src/core/project.ts` — project/assets/tracks/clips/storyboard schema.
- `src/core/timeline.ts` — immutable edit operations incl. track state and clip gain.
- `src/core/timelinePlacement.ts` — safe compatible real-asset placement.
- `src/core/storage.ts` / `persistence.ts` — storage safety + persistent project metadata.
- `src/core/render.ts` / `renderJobs.ts` / `renderUi.ts` — RenderPlan, trackIndex, job lifecycle, readiness/output helpers.
- `src/core/exportHistory.ts` — bounded validated project receipts for successful managed exports.
- `src/core/publishPackage.ts` / `src/components/PublishPanel.tsx` / `worker/publish-package.mjs` — validated reusable per-project defaults, non-overwriting managed JSON sidecars, and bounded project-scoped listing/reopening with live MP4 availability.
- `src/core/localWorker.ts` — safe path mapping, ffprobe parsing, deterministic compositor planning.
- `src/core/workerClient.ts` / `workerProtocol.ts` — authenticated localhost bridge incl. asset upload/render lifecycle.
- `src/core/assetUpload.ts` — browser upload response validation.
- `src/core/mediaImport.ts` — probed media → managed project asset.
- `src/components/AssetUploadPanel.tsx` — explicit browser file chooser → upload → probe → register.
- `src/components/TimelineEditor.tsx` — real timeline controls.
- `src/components/CaptionEditor.tsx` / `src/core/captions.ts` — structured timed caption creation/editing.
- `src/components/AssetPlacementControl.tsx` — compatible track chooser + Add to timeline.
- `src/components/RenderPanel.tsx` — real render start/progress/cancel/result.
- `src/core/versioning.ts` / `src/components/VersionHistoryPanel.tsx` — persistent named snapshots and reversible Studio restore controls.
- `src/core/director.ts` / `src/components/DirectorPanel.tsx` — validated DirectorPlan contract and explicit review/apply UI.
- `worker/ollama.mjs` — localhost-only Ollama discovery and structured Director generation adapter.
- `worker/comfyui.mjs` — localhost-only ComfyUI URL, managed workflow-template binding, generated-image path contract, discovery/history/queue/view runtime helpers.
- `src/core/imageJobs.ts` — typed image-generation availability, cancellable job state and trusted provenance parsing.
- `src/core/generatedImages.ts` / `src/components/ImageStudioPanel.tsx` — once-only provenance-preserving asset registration and the visible local Image Studio workflow.
- `src/core/storyboardFulfillment.ts` — explicit assign/replace/clear of scene-fulfilling visual assets.
- `src/core/videoJobs.ts` — typed cancellable video generation job and provenance parsing.
- `src/core/generatedVideos.ts` / `src/components/VideoStudioPanel.tsx` — once-only provenance-preserving video asset registration and the visible local Video Studio workflow.
- `worker/capture.mjs` / `src/core/captureJobs.ts` — bounded shell-free macOS screencapture commands, managed capture paths and typed real-capture job/provenance parsing.
- `src/core/capturedMedia.ts` / `src/components/CapturePanel.tsx` — once-only registration of real captures and the explicit-authorization Capture Studio.
- `worker/webcapture.mjs` / `src/core/webCaptureJobs.ts` / `src/core/webCaptures.ts` — installed-browser discovery, headless URL screenshot jobs and once-only web capture registration with URL/browser provenance.
- `src/core/mediaAcquisition.ts` / `src/components/MediaPlanPanel.tsx` — schema-validated per-scene acquisition plans from the local model and the review-once/run-automatically workflow.
- `src/components/SceneFulfillmentControl.tsx` — shared explicit scene fulfillment control for generated images and videos.
- `worker/whisper.mjs` / `src/core/sttJobs.ts` — managed whisper.cpp execution contract and typed cancellable job/result state.
- `src/components/SttPanel.tsx` / `src/core/transcripts.ts` — visible STT lifecycle and attributable transcript asset registration.
- `worker/asset-upload.mjs` — safe filename/temp/final upload paths.
- `worker/captions.mjs` — deterministic ASS generation, escaping and managed temp paths.
- `worker/proxies.mjs` / `src/components/VideoProxyControl.tsx` — deterministic managed video proxy generation and association.
- `worker/mac-worker.mjs` — actual local upload/ffprobe/FFmpeg runtime.
- `src/App.tsx` — Projects/Create/Studio/Assets/Settings shell; unfinished sections are not faked.

## Current real end-to-end path
1. Create/reopen persistent project.
2. Connect authenticated localhost worker.
3. Select video/audio/image explicitly in browser OR probe an existing managed asset.
4. Worker streams selected file safely into `KINAOU/Assets`; PWA auto-probes and registers it.
5. Choose compatible timeline track and place asset.
6. Move/trim/mute/lock; reorder visual layers; adjust audio gain, speed and visual position/scale/crop; add dissolve and audio/visual fade envelopes.
7. Create/edit timed Unicode captions stored non-destructively in the project.
8. Render deterministic multi-track visual/audio composition plus caption burn-in to `KINAOU/Renders` with explicit z-order.
9. Generate/play managed source proxies, thumbnails and waveforms while retaining originals as render sources.
10. Render the composed timeline into managed 540p preview cache and scrub its real output, or export full quality to `KINAOU/Renders`.
11. Create named project versions, inspect their summaries, and restore any snapshot reversibly through an automatic pre-restore safety version.
12. Detect an installed local Ollama model, generate a schema-bound DirectorPlan, validate/review it, then apply its script/storyboard with an automatic pre-apply safety version.
13. Watch render progress, cancel, see failure/result, retry as new job; worker cleans caption temp files in every terminal path.
14. Check honest local ComfyUI availability, generate images from managed workflow templates with prompt/seed/dimension control, cancel mid-run, and get each success registered once as a provenance-carrying GENERATED asset with explicit timeline placement.
15. Explicitly assign a generated visual to the storyboard scene it fulfils, replace a fulfilled scene only through an explicit action backed by an automatic safety version, keep alternatives, and clear assignments without losing assets.
16. Generate scene video locally from video-declaring managed templates, cancel mid-run, get each success registered once with real duration and provenance, reuse settings for explicit regeneration, and place clips only through explicit scene/track controls.
17. Capture the real screen only on explicit request — delayed screenshots and stoppable bounded recordings — and get each capture registered once as a REAL CAPTURE asset with capture provenance, scene fulfillment and explicit timeline placement.
18. Capture any explicitly entered website as a real reproducible headless-browser screenshot with URL/browser provenance, and capture a named app's window automatically (activate → resolve bounds → region capture) as screenshot or recording.
44. Run a visible real-file preflight before a local Publish handoff: ffprobe must confirm the recorded file size, video stream, output-format dimensions and duration; a mismatched or post-review-changed MP4 cannot produce a package.
43. Set whole-image or fill-frame treatment plus horizontal/vertical crop focus independently for Landscape, Vertical and Square; the project retains each choice and the same framing reaches timeline preview, reviewed Short preview, full export and every matching batch variant.
42. Save reviewed Publish metadata as defaults for exactly one project, reuse it immediately or after reopening the project, and clear the saved copy without changing the current form or contacting the worker.
41. Refresh Publish's local package library, see live MP4 availability and reopen the reviewed metadata from any validated handoff for the active project without changing either file.
40. Select any recorded successful export in Publish and create a new platform-labelled JSON handoff beside the verified non-empty MP4; the source video stays byte-untouched and no upload or platform API occurs.
39. Preview any selected reviewed Short independently as Landscape, Vertical or Square through the real compositor before export; the project's main format stays unchanged and a format/settings change invalidates the old cached preview.
38. Adapt any reviewed Short selection into one or more Landscape, Vertical and Square variants in a single bounded batch; every variant uses its real full-quality format profile, progress, managed output and persistent success receipt while only one FFmpeg job runs at a time.
37. Check the retained export history against the connected managed drive and see a real per-MP4 available/missing result without changing the receipt or file; reconnecting the drive can be checked again.
36. Reopen the project and retain up to 50 validated successful single/batch export receipts with managed MP4 path, format, exact range, scene provenance, duration, size and completion time; forgetting a receipt keeps the MP4 untouched.
35. Render and scrub the selected Short candidate as the exact composed 540p range before export, with current framing, captions, edits and audio processing; changed settings invalidate the old preview.
34. Choose and persist the project's Short maximum with 15/30/60/90-second presets or an exact 1–600-second value; scene groups are replanned and must be reviewed again before export.
33. Select several reviewed Short candidates and export them as a bounded sequential batch with individual progress/results and cancellation; only one local FFmpeg job runs at a time.
32. Select a reviewed Short candidate and export it under a stable scene/range-specific managed filename; manual range edits clear the candidate identity.
31. Review deterministic Short candidates assembled from contiguous storyboard scenes up to 60 seconds, see exact skip reasons, and feed any candidate into the real In/Out export without changing the edit.
30. Opt into master loudness normalization for an export, with the declared −14 LUFS/−1.5 dBTP/LRA 11 target visible and the original sound path retained by default.
29. Keep music intelligible but out of the way automatically: Render lowers music under voice/dialog with visible, editable reduction/attack/release controls and an explicit off switch.
28. Set explicit In/Out points in Render, see why an invalid range cannot run, and export only the selected interval without changing the timeline; retimed media starts at the correct source moment and a reset returns to the whole timeline.
27. Move a scene's captions back over it after its length changed — same order, same relative widths, the words untouched.
26. Replace a scene's visual after assembling and update it in place — the new media appears inside the clip the scene already has, at the same position and length, instead of a second copy at the end.
25. Fit the scenes to the narration in one action — every overrunning visual grows until it covers its own voice, everything in a later scene moves with it so the cross-dissolves keep their width, and footage that cannot be stretched that far says exactly why.
24. Narrate each scene with the local Piper voice, placed under its own scene, with overruns reported rather than trimmed.
23. Give still scenes a slow zoom and dissolve between them, both applied by the assembly itself and adjustable per clip.
22. Caption the whole sequence from the scenes' own script text, split at sentence boundaries and timed to the clips actually on the timeline.
21. Choose the project's target format — landscape, vertical or square — and both the composed preview and the export follow it; whole-image/fill-frame treatment and crop focus can then be tuned per format.
20. Assemble every fulfilled storyboard scene onto a visual track in one action — in scene order, gapless, stills at their scene duration and videos capped at real footage — then render it. The full Discover→Direct→Generate→Edit→Export path runs without manual per-asset placement.
19. Say what the video is about and let KINAOU do the rest: the local model decides per scene which URL to capture, which app window to capture, or what to generate — either fully automatic with the review on the results, or with an editable plan review first (change/add/remove items, KINAOU re-validates and executes). Both paths register everything with provenance, fill only still-empty scenes and are undoable via the automatic pre-run safety version.

## Current limitations
- UI language switching is real (#187), including navigation, project creation and course forms; Settings (#189), Version History (#191), project backups and global recovery (#193), manual caption editing/transcript import (#195), script-caption/alignment feedback (#197), manual timeline controls (#199), source/composed preview (#201), storyboard assembly/replacement (#203), scene narration (#205), single-export lifecycle/base controls (#207), framing/export-history controls (#209), course-export review (#211), Short selection/recipes (#213), Short previews (#215), and Short archives (#217) include translated result/error feedback. Active Short batch lifecycle controls plus remaining Studio/generation specialist panels still need translation; original technical diagnostics remain untranslated. This is not complete localization. Existing Short range candidates and exports do not yet provide main-video/Short editorial recommendations or evidence-backed publication-time planning. These remain explicit next-work items (CREATIVE_QUALITY.md, UI-LANGUAGES.md).
- User-requested URL video acquisition for analyses/portraits is not implemented. Download authorization, reuse/copyright basis and platform monetization are separate checks. Never infer permission from a clip being public, short or below half of the final video; preserve evidence per source/use and keep unresolved cases for review. See SOURCE-IMPORT.md for the planned workflow and checked official YouTube references.
- Cartoons, children's formats and learning content are explicit product targets, not a shipped dedicated generation/review workflow. Existing prompt/media/editing paths may be used with suitable authorized sources and installed models, but character continuity, animation quality, age-specific review and lesson accuracy need actual validation. No live trend adapter or guaranteed free source of current demand data exists yet; the no-recurring-provider-cost constraint remains in force.
- Course now persists modules and lessons within one project and exports saved lesson ranges individually (#185). It does not yet provide cross-project lesson libraries, lesson script/resource authoring, exercises/solutions, verified demonstration evidence, instructor sign-off or a course publication bundle. Ranges are absolute and require review after timeline edits; the current general export receipt history is bounded to 50. Course language does not automatically translate or change the project voice profile. See COURSE-PRODUCTION.md.
- Local video templates can now receive explicit portrait/speech references, including the user's own recording (#183), with source hashes retained. This is the real transport path to an installed compatible workflow, not an installed avatar model or measured lip-sync quality. Video-reference conditioning and new speech generated in the user's voice remain absent. ComfyUI keeps job-specific input copies after cancellation/completion; the UI explains this and originals remain unchanged. See VIDEO-REFERENCES.md.
- Scene narration/captions now share explicit spoken text for newly applied plans. Previously saved scenes without that field still use their description; existing generated media is not regenerated automatically. Script-caption timing is distributed over the visual scene, not word-aligned to the recording.
- The editor has real underlying operations but remains largely button-driven (including one-second move/trim steps). It does not yet provide a conventional direct-manipulation timeline with trim handles, playhead splitting, frame snapping, multi-selection or immediate composed preview; do not claim MAGIX-level editing ergonomics or feature parity.
- Only dissolve-in transitions are supported; fade automation is limited to bounded clip-edge envelopes. No keyframes. Position/scale/crop are static per clip, but stills can carry a zoom-in/zoom-out over the clip's length.
- Preview is render-then-play rather than frame-live. Source/composed controls are localized (#201); relevant edit/connection changes hide stale playback and each new submission gets a separate cache path. Leaving Studio ends monitoring but does not cancel an accepted worker job. Cache cleanup and cross-navigation job recovery are not added by this slice; files are retained. Full-render/Short controls remain separate work.
- Local Director and AI Editor generation through Ollama, STT through whisper.cpp, TTS through Piper and image generation through ComfyUI are real but require their open runtimes/models already installed on the user's Mac. Generated images and videos register as attributable assets with explicit timeline placement and storyboard-scene fulfillment; regeneration never overwrites existing assets.
- The project has a persistent German/English/French content profile consumed by local Director generation. Piper discovery displays declared voice languages against that profile, but does not verify pronunciation or guarantee synthesis quality. Avatar supports an editable still portrait paired with existing narration through the real preview/export path. It does not animate a face, lip-sync, clone a voice, remove a background or generate a presenter model. Trend discovery has no live data adapter yet; accurate current demand/competition claims require attributable allowed sources and cannot be inferred from a local model alone.
- Browser/app visuals now have both paths implemented and strictly separated: real capture through explicit user-initiated macOS screencapture jobs gated by the system Screen Recording permission (display/region/interactive screenshots incl. on-screen window pick, app-window capture by name, stoppable recordings, headless website captures), and locally generated visuals carrying prompt/model/seed provenance. Generated visuals must never be presented as evidence of a real screen state, and captures always carry real-capture provenance.
- Publish persists reusable project-local form defaults, ffprobe-preflights the chosen export and prepares/reopens real local MP4+JSON handoffs. New packages retain probed media facts plus a SHA-256 digest, and the library can explicitly verify one selected MP4 as unchanged, modified or missing without hashing the full collection; older version-1 packages remain readable but cannot retroactively prove integrity. KINAOU does not claim to upload or publish; provider APIs, credentials, scheduling and remote analytics remain absent until explicitly authorized and implemented.
- Short export batches persist one current queue of at most 100 reviewed range/format attempts with exact resume checks and one-at-a-time local execution. Completed, failed and cancelled entries remain terminal after reload; the latest failed/cancelled variant can be selectively retried under a fresh non-overwriting output identity while earlier attempts stay visible. Replacing or explicitly clearing a fully terminal queue retains a compact summary of the newest ten runs and every recorded output path. One archived batch can be checked on demand for present/missing files against the connected drive, and its complete prior selection can be restored into the current review controls without starting work; unavailable candidates are omitted and incomplete historical matrices are refused. These timestamped checks remain presence-only rather than hash-backed integrity claims. Every MP4 remains untouched.
- The real-Mac execution test (`docs/MAC-TEST.md`, MacBook Pro M2 Pro, exFAT SSD `mypassport`) has passed **all core stages 1–4**: Stage 1 worker connection with honest capability chips; Stage 2 core media path (browser import → probe → timeline placement → trim/fade/speed edits → real FFmpeg render to `KINAOU/Renders`, proxy generation and authenticated source preview); Stage 3 real screen capture with the full TCC flow (Screen Recording grant + worker restart, interactive pick incl. honest Escape cancellation, app-window capture by name with Automation grant, recording with Stop & keep and real duration); Stage 4 website capture with persisted managed PNG and URL provenance. Seven real-hardware bugs were found and fixed along the way (PRs #86, #87, #88, #90, #92, #94, #96). Remaining optional: Stage 5 (local AI runtimes — Ollama, whisper.cpp, Piper, ComfyUI — each requires the user installing that runtime) and Stage 6 (the full Discover→Direct→Generate→Edit→Export loop, which needs Stage 5 components).

## Current next milestone
Advance **real avatar inputs and expressive/own-voice workflows**, retaining the tested local editing/export path.

Prerequisite corrected in #181: Director speech now actually reaches TTS and captions. Authorized portrait/own-recording transport to installed video workflows is implemented in #183, but verified lip-synced performance and generating new speech in the user's voice still require the relevant local adapters and measured model output. Course/module/lesson persistence and individual video exports are now real (#185); next course work is lesson content/resources, demonstration evidence, exercises/solutions and review invalidation after edits. Manual editing remains a parallel usability priority; the core operations exist but the direct editing experience does not yet match a conventional editor.

UI language foundation is merged in #187; Settings, Version History, backups and recovery follow in #189/#191/#193. Manual caption authoring/import is complete in #195, script-caption generation/alignment in #197, manual timeline controls in #199, source/composed preview in #201, storyboard assembly/replacement in #203, scene narration in #205, single-export lifecycle/base controls in #207, framing/export history in #209, course-export review in #211, Short selection/recipes in #213, Short previews in #215, and Short archives in #217. Next work is active Short batch lifecycle/persistence controls and creation panels without translating stored user content or resetting jobs/drafts on language changes. Keep the partial-coverage notice until the full workflow is localized and tested. Main-video/companion-Short planning, evidence-qualified local publication suggestions and cartoon/children/learning production remain next independent slices; none authorizes subscription dependencies, remote publication or invented trends.

Completed foundation: reviewable Long-to-Short ranges and sequential batches (#133–#139), exact range previews and durable export receipts (#141–#145), independent multi-format batches/previews (#147/#149), a real local MP4+metadata handoff in Publish (#151), a validated project-scoped library for reopening those handoffs (#153), reusable per-project Publish defaults (#155), persistent per-format reframing shared by every preview/export path (#157), an enforced real-media preflight (#159), durable on-demand package integrity verification (#161), project-persistent exactly resumable local Short batches (#163), selective non-overwriting recovery of failed/cancelled variants (#165), a bounded visible archive of completed batch attempts (#167), an explicit presence check for one archived batch at a time (#169), review-only reuse of complete archived selections (#171), named project-local Short recipes (#173), and a German/English/French project language/audience profile consumed by local Director generation (#175).

The local handoff boundary is complete without pretending to contact a platform, and the first Content Factory queue now survives reloads, supports deliberate recovery, retains bounded run history, rechecks archived output presence and has named review-only recipes without touching media. Language-aware voice discovery (#177), reversible voiced-still presenter composition (#179), declared managed portrait/audio transport to local video workflows (#183), and the course outline/individual lesson export foundation (#185) are complete. The user's clarified priority is actual photorealistic photo-driven avatars and expressive natural speech, not refinements that present the voiced still or a successful file transfer as the final avatar engine. Next, evaluate an expressive multilingual/authorized-own-voice local adapter behind honest capability detection, and select/validate an actual audio-driven video workflow rather than inventing a runnable model template. Keep large downloads/model installation explicit. Independent progress can extend course lesson content/materials and instructor review or conventional manual editing ergonomics. See CREATIVE_QUALITY.md, VIDEO-REFERENCES.md and COURSE-PRODUCTION.md. Accurate current trend/research signals and provider publishing remain contingent on explicit, allowed data-source/API integrations; they are not simulated.

## Later roadmap
Studio fidelity baseline complete: transforms/reorder → transitions → fades/automation → retiming → proxies/thumbnails/waveforms → composed preview → persistent Version History.
AI creation: DirectorPlan schema → local LLM adapter → script/storyboard/scenes → STT/TTS → image/video adapters → AI Editor structured proposals/diffs/undo.
Advanced: Avatar, Audio Studio, explicitly authorized browser/app screenshot and screen-recording capture, generated UI/site scene workflows, commentary/reaction/duo, long-to-short, content factory, trend/opportunity, platform adaptation, official-API publishing, analytics/learning loop.

## Definition of first major KINAOU core milestone
User can create a project, obtain script/storyboard/scenes, import/generate media/voice/captions, edit a real timeline, AI-edit selected ranges reversibly, render/export, reopen safely, and keep models/projects/assets on external SSD with disconnect/reconnect resilience.

## Working discipline
For every meaningful slice: verify main → feature branch → implement real behavior → tests → PR → green CI → merge → update this file.

User clarification 2026-09-20: continue sequentially without requesting another “weiter”. Notify only when a WHOLE numbered point is genuinely complete (for example “Punkt 1 erledigt”), then continue directly. Do not send substep completion notifications or routine progress updates; interrupt only for a concrete required user action or blocker. Internal substep numbering is implementation bookkeeping, not a user-facing completion schedule. The stable ten-point list, acceptance boundaries and completed substeps live in [COMPLETION_CHECKLIST.md](COMPLETION_CHECKLIST.md). Do not mark a whole point complete for one translated panel or a working adapter contract. Independent work currently continues on point 1; single-export lifecycle/base controls (#207) and framing/export history (#209) are complete internally, not the entire export panel. Next work is active Short batch lifecycle/persistence controls; course-export review (#211), Short selection/recipes (#213), Short previews (#215) and Short archives (#217) are complete internally. Football/sports content is included in point 7 and evidence-based sports research in point 5; authorized URL acquisition and reuse review are tracked in SOURCE-IMPORT.md.

## USER ACTIONS AT END
No user action blocks independent repository work now.
The ordered step-by-step test checklist lives in `docs/MAC-TEST.md`, and the real Mac execution test has passed all core stages 1–4 on real hardware (see Current limitations for the per-stage detail). Seven real-hardware bugs found by it were fixed as PRs #86 (fetch binding), #87 (exFAT browser profiles), #88 (file-based web-capture completion + resilient polling), #90 (honest FFmpeg capability chips), #92 (friendly port-conflict exit), #94 (visible timeline placement errors) and #96 (over-retimed clip crash + app error boundary). Remaining OPTIONAL USER ACTIONS: Stage 5 (install any of the local AI runtimes below and test its workflow) and Stage 6 (full loop).
The Mac execution test requires:
1. Node.js 22+ and FFmpeg/ffprobe in PATH.
2. The external SSD plugged in with a dedicated `KINAOU` directory; launch the worker with `KINAOU_MANAGED_ROOT` plus a token.
3. For real screen capture: grant macOS Screen Recording permission (System Settings → Privacy & Security → Screen Recording) to the terminal/process running the KINAOU worker when macOS prompts on the first capture; app-window capture additionally needs the Automation and Accessibility permissions on first use. Website capture needs any installed Chrome/Chromium/Firefox (or `KINAOU_CHROMIUM`/`KINAOU_FIREFOX`).
4. For image/video generation: a locally installed ComfyUI running on `http://127.0.0.1:8188` (or set `KINAOU_COMFYUI_URL`), with the user's own models installed in ComfyUI, and at least one API-format workflow template JSON (wrapper with `schemaVersion`, `id`, `label`, optional `"mediaType": "video"`, `workflow`, `bindings`) placed under `KINAOU/Models/ComfyUI/Workflows`.
5. For Director/AI Editor: local Ollama with a pulled model; for STT: whisper-cli plus a GGML model under `KINAOU/Models` (`KINAOU_WHISPER_CLI`); for TTS: Piper plus an ONNX voice+JSON under `KINAOU/Models` (`KINAOU_PIPER_CLI`).
KINAOU downloads none of these itself and shows honestly what is missing. Keep independent repo work moving until the Mac test.

# Application languages

PR #187 adds a real, partial application-language layer in German, English and French. It does not claim that every panel has already been translated.

## Current behavior

- The sidebar selector always uses native names: Deutsch, English, Français.
- A valid saved preference wins; otherwise the first supported browser language is used, with English as the fallback. Regional browser locales such as fr-CA and de-DE resolve to their base language.
- Preference storage is a separate `kinaou.ui-language.v1` localStorage key, scoped to the current browser origin. The document's `lang` follows the selection. No server, worker or model is required.
- Switching language does not navigate, remount the app/course form, rewrite projects, translate user text, change course/content language or start/cancel media jobs. Unsaved creation/course edits remain in the current form. Course drafts still require explicit save before leaving that page or reloading.
- A failed preference write keeps the selected language for the session and displays a translated warning. This preference handling does not bypass the existing app-level recovery path for unavailable project storage.
- Course success/error summaries follow the current language. Original technical error details remain available under a translated disclosure heading.

Translated now: navigation, common project-required states, shell labels, project creation, library summary/date labels, reserved-feature explanation, the course-authoring form, Settings (#189), Version History (#191), project backups and global error recovery (#193), manual caption editing/reviewed transcript import (#195), script-caption generation/alignment (#197), manual timeline controls (#199), source/composed preview (#201), storyboard assembly/replacement (#203), scene narration (#205), single-export lifecycle/base format/range/audio controls (#207), framing/export-history controls (#209), course-export review (#211), Short range/format/recipe selection (#213), Short preview sessions/playback (#215), and Short archives/read-only file checks (#217). Active Short batch states/controls/notices are translated in #219; accepted-job monitoring is scoped in #221; receipt persistence/recovery is covered in #223; saved cancellation and guarded initial dispatch are covered in #225/#227; voiced-portrait controls/validation/saved feedback are translated in #229; AI Editor controls/diffs/review/save feedback are translated and scoped in #231; Director review/content profile/speech labels are covered in #233; Media Plan controls/review/acquisition boundaries are covered in #235; Audio Studio and shared asset placement are covered in #237; Image Studio and shared scene assignment are covered in #239; Video Studio and reference permissions/recovery are covered in #241. Original technical diagnostics remain untranslated. An explicit notice states the partial coverage in all three languages. Render directions in course help lead into a partially localized specialist panel.

Settings preserves parent-owned drafts/credentials, disables credential edits while testing a connection, translates connection failures and storage-save results, and keeps raw capability IDs/paths intact. It explicitly distinguishes the saved browser profile from the worker's actual startup root: saving a path does not mount an SSD, create/move files or reconfigure the worker. A failed save keeps the old saved profile and current inputs. The token remains in memory only.

## Extension rules

Use `useUiLanguage().t(...)` and the typed de/en/fr catalog. All entries must have three nonempty values and matching interpolation placeholders. Do not construct user-visible sentences by concatenating untranslated fragments or use labels as internal section/job identities. Keep errors/statuses as message keys plus values when they should update during a language change.

Translate interface text, not stored scripts, course names, narration, filenames, source evidence or consent/provenance data. Newly added editable default lesson/module titles use the UI language at creation; later switching does not rename them. Content-language selection remains explicit. Never key a panel by UI language: that would silently discard its draft or interrupt its state.

## Verification

The feature gate passed 301 application tests, 82 native worker tests, TypeScript/production build and worker syntax checks. Twelve new tests cover saved/browser/default selection, invalid/blocked storage, isolated preference writes, translation/placeholder parity, real App and Course component rendering in all three languages, unchanged course content and corrupt-outline recovery.

An isolated browser at `127.0.0.1:5180` verified project draft retention DE → FR; course title/module/lesson retention FR → EN → DE/FR; separate French course language; translated invalid-title feedback; save/discard; and saved language/course persistence after reload. No regular user-origin projects, worker processes, SSD files or models were changed. The test server/tab were closed afterwards.

Settings gate (#189): 311 application tests + 82 native tests + build/syntax passed. Ten additional regressions cover each language, exact drafts/paths/capabilities, disabled pending credentials, escaped errors and blocked storage writes. The isolated localhost:5181 browser test verified DE→FR→EN draft retention, invalid-URL rejection, translated error/success messages, saved profile persistence and cleared token on reload. No live user worker or SSD was used.

Version History gate (#191): 319 application tests + 82 native tests + build/syntax passed. Eight additional tests cover three-language labels/source/date summaries, preserved original names, escaping, blocked history reads, failed safety writes and reversible snapshots. Isolated localhost:5182 browser verification created a named version, preserved its name draft across DE/FR/EN, restored an earlier project without its later course, then restored the safety version and confirmed the later course after reload. No media files were involved. Action errors clear success messages; technical details remain original. A failure may still leave a safety snapshot, which the UI states explicitly. The pre-existing 100-entry default retention and English stored system-version labels are not silently rewritten.

Backup/recovery gate (#193): 335 application tests + 82 native tests + build/syntax passed. Sixteen additional regressions cover three-language backup/recovery rendering, escaped diagnostics, blocked localStorage access, save/list/restore failure distinctions, actual callback ordering and invalidated late successes/failures. The outer boundary still handles provider failures; the inner one uses the current session language. Recovery never clears storage and warns that unsaved drafts can be lost on retry/reload rather than promising that an arbitrary error did not change data.

Isolated localhost:5183 browser verification used a dedicated temporary worker root (not the SSD): save a real JSON backup, change the UI DE→FR→EN without changing the saved title/path, save a later course outline, restore the original backup, verify the course was removed and the safety version exists, then stop the test worker and verify translated list failure. Both local test processes and the browser tab were closed. Project backups copy metadata only; media/models are not included. Re-saving the same project replaces its JSON backup. Scope invalidation suppresses stale UI results/restores, but does not cancel an already accepted worker disk write; reconnect and list backups to inspect uncertain outcomes. Stored system-snapshot labels remain unchanged.

Manual caption gate (#195): 357 application tests + 82 native tests + build/syntax passed. Twenty-two new regressions cover DE/EN/FR controls, original text/provenance, locked/missing tracks, strict draft timing validation, real render-plan values and calculate→snapshot→persist ordering/failures. Manual creation/editing and reviewed transcript import use explicit error feedback and safety snapshots. The UI never translates authored caption/transcript text; transcript timestamps are absolute timeline positions, not automatically adapted to edited source media. Repeated imports can duplicate segments.

The isolated localhost:5184 browser test retained DE/FR/EN drafts; created 1.25–4s and 4–5s captions with the next start correctly advanced; explicitly saved edited text and verified it after reload; and restored an earlier saved text while an unsaved edit remained. That conflict keeps the draft and disables Save until the user explicitly uses/reviews the saved text. A discovered sibling-key collision was fixed, then the final browser test retained drafts with no warnings/errors. Unsaved drafts do not persist across leaving Studio/reloading, and an externally removed caption removes its editor. Script-caption generation/alignment and the broader Studio controls still need localization. The test tab/server were closed; no worker, SSD or model was needed.

Script-caption gate (#197): 369 application tests + 82 native tests + build/syntax passed. Twelve new tests cover three-language scene timing drift, blocked/empty states, visible unalignable scenes, escaped planning errors, stable generation/alignment reason codes and reversible timing changes preserving narration. Existing English diagnostic strings remain for compatibility; UI reasons use typed codes and values rather than matching those strings. Generation results are published only after persistence succeeds. Feedback is scoped to the exact resulting project and selected track, not a language-keyed remount.

The isolated localhost:5185 UI restored a purpose-built metadata fixture through a temporary worker, generated its missing scene caption from actual narration instead of visual directions, verified translated existing-caption/missing-visual skip reasons, realigned an existing caption from 0–4s to 0–8s and verified unchanged words and persisted timings after reload. The fixture's visual media was explicitly offline: this verifies project/UI behavior, not actual video/model generation. Console was clean. Test tab, server and worker were stopped. Timing remains estimated and requires listening/reading review; this adds neither forced audio alignment nor a premium voice engine.

Manual timeline gate (#199): 379 application tests + 82 native tests + build/syntax passed. Ten new tests cover DE/EN/FR controls/accessible position labels, original media/track names, disabled source-overrun trims/speed reset, minimum/fade/transition bounds, reversible actual edits and calculate→validate→snapshot→persist failure ordering. Stored system-version labels remain unchanged, with the existing bounded history retention. A failed project write can leave its safety version; the error states this without claiming completion. Success is shown only for the exact saved project, and the Studio no longer globally claims AUTO-SAVED while other panels may have drafts.

Isolated localhost:5186 browser testing created a clearly labelled planning placeholder, moved it, changed scale/fade, locked its track and verified disabled controls. DE→FR→EN preserved the authored project/track names and edit values. Reload retained the locked 1–6s clip with scale 1.1 and 0.5s fade; restoring its pre-lock safety version made it editable again. Console was clean, and tab/server were stopped. No real media/model/worker/SSD operations occurred. This remains a schematic button-driven timeline, not a scale-accurate drag editor or a new render engine.

Source/composed preview gate (#201): 402 application tests + 82 native tests + build/syntax passed. Twenty-three new tests cover three-language readiness/controls/lifecycle errors, stale start/poll/blob/source responses, serial polling and exact same-job/file retries. Render readiness has stable reason codes while original English messages remain compatible for other panels. Two concurrent real-worker previews verify red/blue pixels, 1s duration, 960×540 output, unchanged source bytes and the exact authenticated files returned to playback. Each submission has an independent managed cache path rather than the former shared project filename.

Isolated localhost:5187 browser testing used only purpose-made temporary red/blue media and a dedicated worker. Source proxy (6s/160×90) and composed preview (3s/960×540) both loaded with browser-ready media and no playback error. DE→FR during a queued job did not restart it; switching to EN retained a scrubbed 3s playhead. Refreshing then changing the clip to 4s removed the obsolete composed player, and the new render played for exactly 4s. A selected missing proxy showed an escaped loading error whose summary switched back to German. Reload cleared session previews/credentials and correctly required reconnection. Console clean; browser/server/worker stopped. No SSD/model/user-media changes.

Preview limitations remain explicit: render-then-play, not frame-live; changing scope or leaving Studio stops UI monitoring rather than cancelling accepted worker work; cache files stay on disk; browser-native playback controls follow browser localization. A file-loaded message is not a quality review or a promise of supported playback codecs; playback errors have a separate translated message. No new avatar/voice model capability is implied.

Storyboard assembly/replacement gate (#203): 422 application tests + 82 native tests + build/syntax passed. Twenty new regressions cover DE/EN/FR controls, escaped original names/diagnostics, visible unavailable replacements, locked writes, calculate→validate→snapshot→persist ordering/failures, reversible assembly/replacement and legacy scene adoption. A true no-op repeat leaves history unchanged. Overlaps below the schema's 100ms minimum now use a hard cut. Feedback belongs to the exact resulting project and clears on option/track changes; project IDs isolate state without language-based remounting.

Isolated localhost:5188 browser verification used purpose-made temporary red/blue videos and a dedicated worker. DE→FR→EN preserved motion/crossfade choices and original names. Assembly appended one scene; repeating added neither clips nor history. Replacing a 2s scene with 1s footage preserved its start and displayed a shortening warning; unavailable offline replacement remained visible. Safety restoration brought back the prior clip, and reapplying/reloading retained all three clips at 0–1s, 2–3s and 3–5s without stale success feedback. The actual FFmpeg preview was 5s/960×540: blue at 0.5s, black at 1.5s, red at 2.5s and 4s. Browser media readyState was 4 with no playback error; console clean; tab/server/worker stopped. No SSD or user media/model changes.

Replacement deliberately does not move surrounding narration/captions. Shorter footage may leave gaps; incompatible fades/motion/speed can change and transitions need review. Known durations constrain assembly; missing duration metadata is not proof of unlimited footage. A failed project write may retain a safety version, and no success is reported. Stored system-version labels remain unchanged.

Scene narration gate (#205): 445 application tests + 82 native tests + build/syntax passed. Twenty-three new tests cover three-language controls/voice metadata, original text, typed skips, serial jobs, stale start/poll responses, mismatched job identity, history/save failures, same-job retries, uncertain-start non-resubmission, partial-run preservation and reversible fit. The executing authenticated real-worker test uses a declared synthetic tone CLI fixture, never a downloaded model: it verifies exact submitted speech, real 2s WAV, placement/overrun/provenance and no overwrite on repeat. It does not establish speech quality or Piper inference.

Isolated localhost:5189 browser testing used a temporary worker with a delayed tone fixture and explicitly offline visual media. DE→FR while queued retained the selected voice and job, then EN showed only the saved 2s result and silent-scene reason. Repeat added no second voice asset; fit extended the visual and moved the later clip. Restoring pre-narration history, starting again and moving a clip detached the session: the worker produced its second WAV, but the late result did not overwrite the new edit or add a voice clip. Reload retained the edit and correctly required reconnection. Console clean; tab/server/worker stopped.

Narration boundaries: leaving Studio, editing or changing connection detaches UI monitoring/insertion, not worker execution. Files and earlier saved scenes remain. Retry of a known job/status/save does not resynthesize; uncertain start acceptance requires worker inspection before a deliberate new run. History restores project metadata, not file deletion. Fit may move clips on other tracks; review captions, transitions and absolute course/export ranges afterwards. Piper is explicitly a baseline, not expressive/own-voice synthesis. Technical diagnostics and stored system-version labels stay original; shared voice selection defaults to English in the still-untranslated Audio panel.

Single-export gate (#207): 466 application tests + 82 native tests + build/syntax passed. Twenty-one added regressions cover DE/EN/FR base controls and lifecycle feedback, escaped diagnostics, serial polling, concurrent-run protection, stale scope responses, mismatched job/path rejection, retained later edits, failed receipt writes, same-job retries and cancellation races. Two real-worker FFmpeg exports verify separate 1920×1080 red/blue videos, 1s duration, exact original receipts and unchanged source bytes.

Isolated localhost:5190 browser testing used generated test-pattern media and a dedicated temporary worker. DE→FR retained the 0–4s range; switching to EN while queued preserved the job. The completed MP4 was independently probed at 4s/1920×1080. A second whole-timeline submission received a confirmed cancellation and did not add a success receipt or overwrite the first file. Reload retained the first receipt and correctly required reconnection. Console clean; tab/server/worker stopped.

Single-export boundaries: each start captures its own plan and original receipt metadata, while later same-project edits remain intact. Status/save retries use the same accepted job/result, not a second render. Unknown submission acceptance blocks automatic retry; users must inspect the worker/output before a deliberate new submission. Leaving Studio or changing project/connection detaches monitoring, not worker execution; accepted files can remain without a receipt, and single jobs are not recovered automatically after reload. Receipt success does not imply an on-demand presence/quality check. Original stored receipt labels and diagnostics are not rewritten. Framing and receipt-list/file-check localization follows in #209; Course-export review follows in #211; Shorts specialist controls still need localization. This is not complete export-panel coverage.

Framing/export-history gate (#209): 482 application tests + 82 native tests + build/syntax passed. Sixteen added regressions verify DE/EN/FR controls and original receipt labels, independent serialized framing/preset values, read-only unique-path counts, retry, invalid responses, overlapping requests and late success/failure suppression. The actual worker integration checks two existing outputs and one absent path, then forgets a receipt and verifies every MP4 byte remains unchanged. Native FFmpeg coverage proves left/right focus selects different source pixels.

Isolated localhost:5191 browser verification used only temporary generated striped video, a real worker and explicitly present/missing fixture receipts. DE→FR→EN retained crop values and original names, and file checks reported the correct counts without changing media. Two vertical UI exports selected red and blue source regions respectively (sampled RGB 254/0/0 and 0/0/255). Forgetting an existing-file receipt removed only metadata; its SHA-256 stayed identical. Reload retained right focus and the remaining receipts while clearing presence results and requiring connection. Reset restored the format default; other formats stayed independent. Console clean; browser and test processes stopped.

File-check results belong to the exact project, connection and ordered path list. Changes, including a scope round-trip, invalidate late responses and hide previous results; language switches alone preserve the check. Presence is a timestamped observation, not content integrity, media quality or a promise that files remain mounted. Failed persistence is not reported as a successful framing change or receipt removal. No automatic cleanup or media deletion is added.

Course-export review gate (#211): 498 application tests + 82 native tests + build/syntax passed. Sixteen added regressions cover original names and translated selection/eligibility/recovery controls in all three languages, project/range/revision matching, removed/ineligible choices, and stable range reason codes while retaining legacy diagnostics. An invalidated lesson cannot silently fall back to a custom export. Explicit re-review or release is required; deliberate manual In/Out, whole-timeline and Short selection release the lesson context.

Isolated localhost:5192 retained a selected 1–3s lesson through DE→FR→EN without translating its original names or French course language. An out-of-timeline lesson stayed disabled. Trimming the timeline from 4s to 3s invalidated the reviewed range and disabled export; explicit re-review allowed the actual 2s/1920×1080 MP4. Its receipt preserved the original course/module/lesson, revision 1 and range, including after reload. A manual Out edit cleared the selection. Console clean; temporary worker/browser/server stopped. Session review is not a persistent approval and does not validate instructional content or every semantic edit.

Short selection/receipt gate (#213): 506 application tests + 82 native tests + build/syntax passed. Eight new tests cover translated controls and original escaped names, exact fractional skip limits, regrouped recipe availability, portable worker results, wrong job/output and incomplete-success rejection. An executing authenticated FFmpeg test validates a real absolute worker result, portable saved receipt, reload without unfinished plans and archival.

Isolated localhost:5193 retained candidate/format selection and an unsaved recipe name through DE→FR→EN. A zero limit stayed unapplied with translated validation. A 1.501s maximum displayed its exact locale-specific value and skipped rather than truncated 2s scenes. Regrouping invalidated the saved candidate; explicit review showed unavailable ranges without starting export. Restoring the original maximum made the recipe available again. Two fresh 2s MP4s were independently measured at 1920×1080 and 1080×1080; both terminal results survived reload and archive. Reviewing the archive only selected current candidates/formats. Source bytes remained unchanged, and the corrected run produced no new console errors. Temporary worker/server/tab stopped.

The first browser run exposed an existing absolute-path persistence fault and React update-loop diagnostics. The fix normalizes only a matching canonical absolute/relative output and validates complete terminal receipts before state changes; it does not trust another file or relax project schema. Previously unpersisted jobs are not retrospectively invented as successful; an existing output is not overwritten by a stale queued entry. This slice does not overhaul batch concurrency/session scoping or localize its job/archive lifecycle, which remain next work. Candidate grouping is not semantic excerpt quality, measured trend advice or a platform eligibility guarantee.

Short preview gate (#215): 523 application tests + 82 native tests + build/syntax passed. Seventeen added regressions cover serial/submission guards, late start/poll/blob/cancel suppression, mismatched job/path rejection, status/file retries without new work, uncertain start non-resubmission, cancellation/completion races and translated recovery. An executing test creates two independent actual 1s/540×960 range previews, validates red/blue pixels and authenticated blob bytes, and preserves the source.

Isolated localhost:5194 verified German start, French queued status and English loaded playback preserving a 1.25s playhead in a 2s portrait preview (readyState 4, no playback error). A transform hid the loaded preview; changing a transform during a new queued job prevented its late finished cache file from reappearing. Four separate starts retained four independent cache files. The very fast browser cancellation raced completion and correctly displayed loaded success, not a false cancelled state; deterministic tests cover confirmed cancellation and failure/retry. Reload cleared preview/credentials and required reconnection. Console clean; temporary browser/server/worker stopped; source SHA-256 unchanged.

Preview scope includes the complete prepared plan, project and connection, not language. Scope changes stop UI monitoring, not accepted worker work, and cache files stay on disk. Retry of known status/file/cancel failure stays on the same job; an uncertain initial acceptance offers an explicitly labelled new submission with a separate file. Real-time playback generation, quality approval and resumable previews after reload are not claimed.

Short archive gate (#217): 533 application tests + 82 native tests + build/syntax passed. Ten added tests cover translated summaries/states/actions with original escaped titles, explicit unchecked state, metadata-only forgetting, 100 paths checked in two authenticated bounded chunks, no partial counts after later failure, and real scope-key invalidation for project/connection/path changes including A→B→A and unmount. Scope identity is separate from language; late stale reads cannot revive old results.

Isolated localhost:5195 used an explicit synthetic archive with two real test MP4s and one absent path. The real worker reported two present and one missing; DE→FR→EN retained the expanded entry, original names and check time. Reload cleared presence and credentials without losing archived metadata. Reviewing only selected one current candidate and three formats, without exports. Forgetting removed only the summary, including after reload; both original MP4 SHA-256 values remained identical. Console clean; temporary browser/server/worker stopped. Final counter wording uses neutral labels for both singular and plural values.

Presence remains a timestamped read-only observation, not integrity or quality. Removing an archive does not delete files, and a failed project write is not labelled successful. Active batch submission/polling/persistence recovery and their remaining English controls are still separate next work; whole interface completion is not claimed.

Short batch controls gate (#219): 543 application tests + 82 native tests + build/syntax passed. Ten new tests verify saved-first prepare/retry/discard using the actual project repository, no activation on failed saves, preserved recovery state and DE/EN/FR rendering of every state and retained notice. Original names, paths and expandable diagnostics stay unchanged. Automatic persistence only advances its reference after a successful write.

Isolated localhost:5196 switched to French during running/queued work and English after completion. Two real outputs measured 2s at 1920×1080 and 1080×1080. Reload retained both terminal results while clearing connection credentials. Source bytes remained unchanged; console clean; temporary worker/browser/server stopped. Persistence-failure behavior is covered by tests, not by deliberately exhausting browser storage.

This is not a full batch recovery overhaul: serial status reads, stale dispatch/status/cancellation responses, uncertain initial acceptance and retryable receipt persistence remain follow-up work. A stored terminal result does not itself verify current file presence or media quality. Whole point 1 remains open.

Short batch monitor gate (#221): 562 application tests + 82 native tests + build/syntax passed. Nineteen added regressions cover serial reads, same-job transient recovery, detached late status/file/cancel replies, cancellation races and failure, mismatched job/path/availability and translated pending cancellation. The authenticated real-worker receipt integration uses the production monitor and still verifies an actual 1s/1920×1080 output.

Isolated localhost:5197 confirmed cancellation of one accepted job plus five queued variants, French terminal states, then a selective retry of just the first landscape variant. The retry used a new file and measured 4s/1920×1080. English feedback and reload retained six cancellations plus one success and its receipt. Console clean; source SHA-256 unchanged; temporary worker/browser/server stopped. An initial accessibility-targeting mismatch did not submit work; final verified actions used exact DOM controls.

Leaving/changing scope invalidates responses, not remote execution. Cancellation confirmation may report actual success if completion wins; failed cancellation resumes monitoring of the same known job. Superseded network reads may finish but cannot publish. Existing output presence is not integrity/quality. Initial dispatch uncertainty, cancellation persistence and receipt-save failure handling remain separate work; full point 1 is not complete.

Short receipt recovery gate (#223): 579 app tests + 83 native tests + build/syntax passed. Seventeen new app cases cover single-write receipts/acknowledgements, actual repository reload, save failures and same-result retry, deliberate forgetting before/after migration, 100-attempt/50-receipt bounds, mismatched and incomplete attempts, malformed ledgers and DE/EN/FR recovery. No worker call occurs during save-only recovery. The actual FFmpeg integration also verifies forgetting/reload and unchanged MP4 bytes.

Isolated localhost:5197 reused the seven real test attempts from #221. An initial removal result prompted explicit atomic acknowledgement in the forget action, including pre-migration projects. Final EN→FR and reload retained the successful attempt plus six cancellations while the deliberately removed history receipt stayed absent. MP4 SHA-256 was unchanged; console clean; test processes stopped. Save-failure cases are deterministic repository tests, not forced exhaustion of browser storage. The synthetic project backup used for diagnosis stayed inside the temporary test root.

The first CI gate cancelled an older native audio test because its cleanup could miss an already-emitted process-close event; the test-only lifetime helper and actual early-exit regression fix that cleanup race. Final dbbc6df CI is fully green. A separate local native run found the default Mac FFmpeg lacks the subtitles filter: 81 passing, one caption subtest and its parent failed. Do not conflate CI caption success with local caption acceptance; see MAC-TEST. No production worker changes or installations were made.

Receipt acknowledgements are project metadata, not proof of current file presence or integrity. Existing accepted jobs may continue while receipt storage fails; the recovery button does not render again. A failed write retains unacknowledged results in the current session, but reloading can only recover previously persisted batch data. Initial dispatch acceptance and cancellation persistence still require follow-up; whole point 1 stays open.

Voiced-portrait UI gate (#229): 622 app + 83 native tests and build/syntax passed. Fourteen new UI/core regressions cover all three languages, original source labels, missing media, stable reason codes, safety/project write failures and reversible composition. A new executing FFmpeg integration saves/reloads a real still plus 2.345s test tone, verifies full audio/video streams and unchanged source bytes, then restores the pre-add history. Test audio is a generated tone, not evidence of natural speech quality.

Isolated localhost:5198 retained an original name and both source selections across DE→FR→EN, then translated the successful-save message into French. Two ordinary tracks survived reload and were reversed through Version History. A real UI export measured 1920×1080 with 2.345011s audio; console clean and temporary processes stopped. Content labels and stored system-version labels remain original. The name is a draft until adding; switching language does not reset it, while leaving the panel/reloading is not draft persistence. No avatar animation, lip sync, own-voice synthesis or model installation is added.

AI Editor gate (#231): 638 app + 83 native tests and build/syntax passed. Sixteen new regressions cover selected-only reversible commits, changed project/target rejection, failed history/project writes, scope supersession/source edits/unmount/StrictMode reattachment and translated controls/diffs. The executing authenticated FFmpeg integration applies a manual reviewed 0.5 gain edit and measures decoded audio RMS between 0.47 and 0.53 of the original, retaining original source and first-export bytes. This tests the actual editing/render path, not local-model reasoning or speech quality.

Isolated localhost:5198 retained JSON, instruction and selection through DE→FR→EN. A manual move disabled the stale selection; re-review displayed the new 1,000ms baseline. Saving moved the caption to 500ms, translated success into French and survived reload. Restoring its safety version returned to 1,000ms without changing original caption text. Console clean; temporary server/worker/tab stopped. No real model was installed or invoked; deferred-response lifetimes and persistence failures are deterministic tests.

Project/connection changes or leaving invalidate pending replies but do not cancel already running inference. Editing JSON also detaches a pending reply. Drafts are not promised to survive leaving/reload. Review comparisons are per-operation against the reviewed baseline; the selected combination is validated before any write. Original user text and technical diagnostics are never translated. A failed project write can leave a safety version, not a successful-edit claim.

Director/profile gate (#233): 653 app + 83 native tests and build/syntax passed. Fifteen added regressions cover original scripts/scenes/speech, translated review controls and required-media labels, profile language independence, reversible persistence and failed/stale commits. An in-process save/reload pipeline verifies the original narration reaches voice planning and caption generation rather than speaking visual directions. This is not model-quality evidence.

Isolated localhost:5199 kept original audience, creative brief, JSON and French content language through DE→FR→EN. Profile saving invalidated a previously reviewed plan; saving remained blocked until re-review. Invalid-market feedback translated while retaining the draft. The full script and silent-scene boundary were visible. A saved plan survived reload; Version History restored the original storyboard while retaining the previously saved profile. Console clean; temporary server/tab stopped. No live model was invoked or installed; response lifetimes reuse the tested AI Editor scope. UI detachment is not inference cancellation.

Director applies the whole reviewed script/storyboard, never media or timeline replacement. Existing clip/scene associations still need review after a new plan. Profile changes only guide subsequent local generation; they do not translate existing scripts or synthesize voices. Drafts are not autosaved on leaving/reload. Media Plan below Director is a separately scoped panel, covered next in #235; whole point 1 is not complete.

Media Plan gate (#235): 668 app + 83 native tests and build/syntax passed. Fifteen added deterministic tests exercise serial saved-first results, preservation of fulfilled scenes, reversible history, history/project/summary write failures, project/connection changes, explicit detachment, round trips, polling identity and DE/EN/FR rendering. These explicit generated-asset test records do not demonstrate model inference.

Isolated localhost:5200 used a clearly labelled fault-only HTTP fixture, not a real model. Review fields retained original positive/negative prompts across DE→FR→EN. An unavailable image runtime stopped after one availability check, before any job submission, preserving the review draft. A delayed automatic plan arriving after a saved profile change was ignored without acquisition and displayed the translated detach boundary. Test server/tab stopped; no user media, model downloads or SSD operations.

Media Plan saves a safety version before acquisition and each asset before showing saved success. Errors stop later items; previously saved assets remain, and files/jobs may exist even when registration/persistence failed. Explicit stop-monitoring, project/connection changes and leaving invalidate the UI run without cancelling accepted work. No automatic resubmission or durable recovery is claimed; inspect worker outputs before a deliberate retry. Drafts are retained on failure in the current panel, not promised across navigation/reload. Technical details, original prompts/URLs/app names/rationale and provenance remain original. Negative prompts unsupported by the selected local template fail before submission rather than being silently dropped.

Audio Studio gate (#237): 694 app + 83 native tests and build/syntax passed. Twenty-six new regressions cover original immutable narration/voice, one submission, serial same-job status recovery, unknown acceptance, history/project write failures, explicit detach and project/connection round trips, mismatched IDs/voices/paths/measurements, cancellation races and translated controls/statuses/placement. The authenticated real-worker integration uses an explicit synthetic-tone CLI to generate a real WAV, fail project persistence once, retry only its save, reload attributed media and restore history while preserving bytes. This proves the workflow, not speech quality.

Isolated localhost:5201 verified a German start and French queued state followed by English saved feedback. Editing the next narration draft did not alter submitted words or provenance. A second job confirmed cancellation. Adding the first result to the timeline during a third job detached monitoring; the later file remained on disk but was not registered. Reload retained one asset/clip; restoring the pre-save version left the actual 2.000000s WAV unchanged (SHA-256 checked). Console clean; temporary server/worker/tab stopped. No real speech model, user media or SSD was used.

The generated file is not reported as saved until both safety history and project persistence succeed. A failed save can leave a safety version and an unregistered file; save-only retry does not synthesize again. Pending status and cancellation retries stay on the accepted job; unknown initial acceptance blocks another start until deliberate detachment, with an inspection warning. Scope changes and navigation stop monitoring, not accepted work. Reload does not recover pending jobs or drafts. Piper is explicitly a baseline without emotion control or own-voice synthesis; existing own recordings remain a separate import workflow. Whole point 1 stays open.

Image Studio gate (#239): 739 app + 83 native tests and build/syntax passed. Forty-five additional tests cover draft boundaries, unsupported negative prompts, immutable prompt/template/seed provenance, serial polling, one submission, unknown acceptance, save-only recovery, cancellation races, project/connection round trips and translated states/scene controls. Clearing a scene visual preserves narration, including explicit silence; assign/replace/clear save history before changing the project.

An authenticated actual worker with an explicit PNG protocol fixture (not model inference) exercises generated-file registration, a failed project write followed by save-only retry, reload, reversible scene/history changes and a real FFmpeg render whose decoded pixel is checked. Original PNG bytes remain unchanged. This is execution evidence, not generated-image quality evidence.

Isolated localhost:5202 verified German invalid-dimension blocking, French/English job states, next-draft editing without rewriting submitted provenance, translated scene assignment/clear, and retained assets/cleared assignment after reload. The initial incomplete protocol fixture correctly produced a worker failure; correcting its queue response allowed success. Console clean; temporary servers/worker/tab stopped. No user media, models or SSD involved. In-flight jobs and drafts are not recovered after reload; detach stops monitoring, not accepted work. Whole point 1 remains open.

Video Studio gate (#241): 791 app + 83 native tests and build/syntax passed. Fifty-two added tests cover scoped immutable jobs, draft validation, duplicate-start/cancel protection, status/save-only recovery, unknown acceptance, reference permissions/identity/hash continuity, actual output measurements and DE/EN/FR states/inputs. Initial acknowledgement may precede reference upload; a successful result must carry all authorized sources. Reuse only prepares fields and clears reference selection/permission, even for the same template.

An actual authenticated worker with a clearly synthetic speech reference and explicit MP4 protocol fixture verifies uploaded bytes, retained source hash, save-only retry, repository reload/history and a decoded-pixel-checked FFmpeg render. It demonstrates the transport and editing workflow, not model inference, expressive speech or lip sync. Isolated localhost:5203 verified DE blank-seed blocking, FR queued and EN running/saved feedback, next-draft separation, reuse without submission, and timeline editing detaching a later result. Reload retained one asset; the detached output remained on disk without registration. Original MP4 hash unchanged; console clean and temporary processes/tab stopped. No downloads, user files or SSD operations. Own-voice recordings are supported references, not voice cloning; unfinished monitoring/drafts are not restored after reload. Whole point 1 remains open.

Next coverage: Assets/import/availability → capture/publishing. Add interaction coverage for each migrated panel, especially pending jobs, errors and drafts. Completion numbering: COMPLETION_CHECKLIST.md. User-facing notifications now refer only to completed WHOLE points; internal substeps do not trigger completion notifications.

# KINAOU numbered completion checklist

Stable numbering from the user-facing status report on 2026-09-20. The user requested autonomous sequential continuation and a “Punkt x erledigt” notification at each completed point. Completion point 1 is complete; points 2–10 remain open. Updated user instruction: do NOT report narrower achievements or routine substep progress. Only notify “Punkt 1 erledigt” once the entire point is complete, then proceed without another signal; otherwise notify only for a concrete user action/blocker. Substeps below are internal bookkeeping with no promised fixed total. Each implementation slice and its separate documentation PR must pass the full gate and merge first.

1. **Complete usable DE/EN/FR interface — complete.** Translate all controls, states, help and recovery paths; make the primary production journey understandable and test it with drafts and active jobs. Completed: 1.1 persisted language/navigation/Create/Course foundation (#187); 1.2 connection/storage Settings and feedback (#189); 1.3 Version History actions, summaries and failure handling (#191); 1.4 global error recovery and project backup (#193); 1.5 manual caption authoring, text-save recovery and transcript import (#195); 1.6 script-caption generation/alignment feedback (#197); 1.7 manual timeline controls, safety history and saved/draft guidance (#199); 1.8 source/composed preview controls and scoped loading/job/error states (#201); 1.9 storyboard assembly/replacement controls with reversible saved changes and visible unavailable replacements (#203); 1.10 scene narration controls, isolated jobs and truthful saved/error feedback (#205); 1.11 single-export lifecycle/base format/range/audio controls with isolated serial sessions and same-job retries (#207); 1.12 framing and export-history controls with scoped read-only presence checks (#209); 1.13 course-export selection, translated eligibility and stale-review guard (#211); 1.14 Short range/format/recipe selection with exact skip limits and portable validated real-worker batch receipts (#213); 1.15 isolated exact Short previews, translated lifecycle/playback and same-job recovery (#215); 1.16 Short archive controls and scoped read-only presence checks (#217); 1.17 active Short batch states/controls/notices and saved-first prepare/retry/discard (#219); 1.18 scoped accepted-job monitoring and cancellation races (#221); 1.19 save-only receipt recovery and durable acknowledgements/forgetting (#223); 1.20 saved cancellation intent with truthful in-flight/actual-worker results and reload-safe blocking (#225); 1.21 write-ahead Short dispatch markers, scoped acknowledgement lifetimes and translated unconfirmed acceptance (#227); 1.22 voiced-portrait controls, validation, save/recovery and retained results (#229), explicitly not avatar animation or voice cloning. 1.23 AI Editor controls/diffs, scoped replies, stale-review blocking and reversible saved changes (#231). 1.24 Director review, full script/visual/speech inspection, content profile and saved/stale feedback (#233). 1.25 Media Plan controls/review, scoped serial acquisition, safe persistence and truthful detach/failure boundaries (#235). 1.26 Audio Studio controls/job recovery and shared asset placement, scoped text/voice identity and save-only retry (#237). 1.27 Image Studio controls, scoped provenance-checked jobs, save-only recovery and reversible scene assignment/clear retaining narration (#239). 1.28 Video Studio controls, scoped provenance/reference-checked generation, recovery and explicit reference permissions (#241). 1.29 Explicit file import, project/connection scope, truthful saved feedback and same-copy probe/save recovery (#243). 1.30 Media availability controls, scoped complete batched checks, saved flags and truthful timestamped summaries (#245). 1.31 Existing-media inspection/review, stale-result guards, exact-root/stream validation and reversible registration (#247). 1.32 Transcription controls, scoped discovery/jobs, same-job status/cancellation recovery and reversible save-only retry (#249); DE/EN/FR static rendering tested, live-browser and real whisper.cpp acceptance not performed in this slice. 1.33 Thumbnail, waveform and proxy controls with DE/EN/FR lifecycle/errors, project/connection-scoped preview-cache sessions, validated cache paths/results, blocked automatic retry after unknown generation acceptance, safety-versioned save-only recovery and image reload/object-URL cleanup (#251); recovery is session-only, no new live-browser acceptance was performed, and safety versions restore project references rather than overwritten cache files. PR CI 35630022907 on `17ff287` and main CI 35630798494 on merge `3817281` passed 934/934 application tests, 83/83 worker tests, production build and worker syntax checks. 1.34 Project asset list with DE/EN/FR section/worker/empty/status/type/classification labels (#253); original asset names, labels, URIs and project data remain unchanged, existing preview/placement controls retain their behavior, and six new static-render regressions cover populated and empty lists in all three languages. PR CI 35632916696 on `64a611d` and main CI 35633137584 on merge `2f58712` passed 940/940 application tests, 83/83 worker tests, production build and worker syntax checks. No live-browser acceptance was performed for this slice. 1.35 Explicit screen/app/website Capture UI with DE/EN/FR availability guidance, inputs, help, lifecycle states, actions, browser discovery, fallback errors and captured-media library (#255); original URLs, app names, paths, captured-media names and provenance remain unchanged, worker-returned technical errors stay original, and worker behavior/permissions/media files are unchanged. Six new static-render regressions cover the three UI languages and capability guidance. PR CI 35640726471 on `7a8931c` passed 946/946 application tests, 83/83 worker tests, production build and worker syntax checks; main CI 35641263465 on merge `a80e349` passed all 96 application-test files plus 83/83 worker tests, production build and worker syntax checks. No new live-browser or real-capture acceptance was performed for this localization slice. 1.36 Local Publish handoff with DE/EN/FR handoff status, destination/form controls, reusable project defaults, export preflight, package creation, local package library, source availability, integrity states, platform labels and render-format labels (#257); original export labels, titles, descriptions, tags, paths, hashes and stored package metadata remain unchanged, worker-returned technical errors stay original, and Publish schemas/worker endpoints/ffprobe/hash/package-file behavior are unchanged. Six new static-render/message regressions cover all three UI languages and metadata preservation. PR CI 35649660776 on `6437ce8` and main CI 35649984951 on merge `f5d95e7` both passed 952/952 application tests across 97 test files, 83/83 worker tests, production build and worker syntax checks. No new live-browser, ffprobe-media, hashing or package-file acceptance was performed for this localization-only slice. Next: remaining specialist panels. Automatic unknown-job retrieval/worker-side idempotency remains absent; legacy unmarked uncertainty cannot be inferred. This does not yet localize the entire export panel. Piper remains the baseline, not the final natural/own-voice target. Completed after 1.36: 1.37 remaining shell/recovery feedback plus language-reactive retained recovery state (#259), preserving arbitrary technical diagnostics and Short-batch safety blockers; PR CI 35653497222 and main CI 35653664413 passed 961/961 application tests across 98 files plus 83/83 worker tests, build and syntax. 1.38 recognized persisted KINAOU system labels (#260), translating automatic history labels, KINAOU whole/custom export labels and exact default track names only at display time without rewriting storage; PR CI 35655294289 and main CI 35655438030 passed 976/976 application tests across 99 files plus 83/83 worker tests, build and syntax. Final acceptance (#261) removed the temporary partial-coverage notice only after a cross-workflow DE → FR → EN gate reused unchanged drafts/authored content across shell, Settings, Course, Timeline, Assets and Publish and exercised active/recovery job feedback alongside the existing multilingual lifecycle suites. PR CI 35657095575 and main CI 35657251441 on `f6b8232` passed 978/978 application tests across 100 files, 83/83 worker tests, production build and worker syntax. Technical diagnostics, paths, identifiers, provenance and user/model-authored content intentionally remain original. **Whole completion point 1 is complete.**
2. **Comfortable manual editing — complete.** Direct dragging, real trim handles, playhead split, 100 ms/grid and clip-edge snapping, atomic multi-selection movement, explicit selection clearing, session Undo/Redo, shared Studio playhead, render-backed linear start/end transform keyframes and a cache-backed immediate visual editing preview are implemented. Completed in slices: direct drag (#263), trim handles (#264), playhead split (#265), multi-selection/group movement (#266), session Undo/Redo (#267), shared Timeline/composed-preview playhead (#268), transform-keyframe workflow (#269), and immediate proxy/thumbnail editing preview (#270). The live editing preview responds without submitting FFmpeg work to current timeline geometry, video source time, static transforms, linear transform keyframes, still-image zoom and fade opacity; the composed FFmpeg preview remains the quality check for captions, exact dissolves, audio and final image quality. Final acceptance covers the editing chain end-to-end and preserves original source media. **Whole completion point 2 is complete.**
3. **Natural expressive and own-voice speech — complete.** Real local Chatterbox Multilingual 0.1.7 synthesis and explicitly authorized own-voice cloning are integrated through the generic speech worker and were tested on supported Apple M2 Pro hardware. DE/EN/FR outputs were reviewed by the reference speaker across separate jobs and scenes. Voice identity was accepted as sufficiently stable; German and English quality were accepted, and the remaining slight non-native French accent was explicitly judged acceptable rather than represented as native French speech. Measured internal tempo defaults are DE 1.25x and EN/FR 1.20x. Non-destructive Audio Studio and per-scene retakes retain distinct jobs, seeds and files under a checked continuity lineage; one scene take is active at a time and earlier takes remain selectable. Retakes correct generation-specific pause/prosody defects without treating retries as new synthesis. Unsupported style-instruction and user pace-control capabilities remain unadvertised. **Whole completion point 3 is complete.**
4. **Persistent avatars and character studio — open.** Target product is a reusable high-end character system rather than a talking-photo feature: preset-, prompt-, image-, video- and multi-reference identity creation; persistent Avatar Library; non-destructive character versions and targeted edits; optional voice binding; reusable scene instances with environment, motion and expression direction; consistent identity across shots; real animation and lip sync; actual output review required. **4.1 foundation complete:** schema-1 projects now carry backward-compatible persistent avatar identities, versions and scene instances; Avatar Studio replaces the old voiced-still panel as the primary Avatar surface; source references remain traceable; imported image/video identity sources require an explicit own/authorized rights confirmation stored with the avatar source. No real identity-generation, motion or lip-sync engine is claimed by this foundation. Whole completion point 4 remains open. **4.2A provenance/engine contract complete:** generated avatar takes now have a typed engine descriptor, declared capabilities and a rights snapshot that keeps private use, commercial output, commercial software use and model redistribution separate. Commercially intended avatar content is rejected when commercial-output permission is not explicitly recorded as allowed. Generated takes can be attached to an exact avatar/version/scene lineage and receive a durable KINAOU creation receipt carrying job/seed, prompt/edit instruction, source asset lineage, source SHA-256 slots, output SHA-256 slot, engine/model versions and license snapshot metadata. Derived identity versions require identity-preservation and targeted-edit capabilities rather than silently accepting a generic generator. Actual file hashing/export and a real identity engine remain subsequent work. **4.2B cryptographic evidence complete:** the local worker now hashes actual managed avatar source and output files with SHA-256 using the same change-detection safeguards as KINAOU publish integrity. Avatar creation receipts can be exported as immutable managed JSON sidecars under `KINAOU/Receipts/Avatars/`; the exported receipt itself is SHA-256 hashed. Re-export is idempotent while evidence is unchanged and is rejected if the existing receipt no longer matches current source/output bytes. Verified source/output hashes and receipt-file identity can be persisted back into the project receipt. Real identity generation, motion, expression and lip-sync engines remain subsequent work.
5. **Research and trends — open.** Attributable current observations by date, country, language and subject including sports; source review and honest demand/competition limits. No live trend adapter exists. Local-model ideas cannot be labelled measured current trends.
6. **Main video plus companion Shorts — open.** Reviewed self-contained excerpt/intro suggestions, titles/descriptions/hashtags and local publication plans with region/timezone and evidence-qualified timing. Real range/batch exports exist, but the full editorial adviser does not.
7. **Cartoons, children's, learning and sports formats — open.** Consistent characters/style, explicit audience/age/purpose, educational accuracy and quality review; football/sports analyses, tactics explanations and short formats with original/authorized sources. Requested acquisition of online video excerpts needs separate download/reuse review and preserved source evidence (SOURCE-IMPORT.md); no percentage-per-source permission rule. No dedicated complete engine/workflow is claimed for these categories.
8. **Complete course production — open.** Modules/lessons and independent video exports exist. Lesson scripts, executed demonstrations, exercises/reviewed solutions, resources, instructor review, revision invalidation and final packages remain. No guarantee of Udemy acceptance.
9. **Publication and analytics — open.** Local packages/preflight/integrity exist; explicit approved platform connections, scheduling and attributable performance feedback remain. No account credentials, upload authority or paid provider use is inferred from this checklist.
10. **Real-Mac whole-product acceptance — open.** Core Mac/SSD/capture stages passed. Actual model/audio/avatar samples, longer productions, performance/memory limits, storage resilience and whole-workflow recovery still need verification. Ask for concrete local actions only when required; never silently download models.

Cross-cutting constraints: no mandatory recurring provider subscription, paid/cloud inference or production deployment; preserve foreign files and authorized source provenance; no fabricated trends, quality guarantees, viral outcomes or platform approval. Local compute still uses electricity/storage/hardware. Keep independent repository work moving while optional hardware/model stages await the user.
